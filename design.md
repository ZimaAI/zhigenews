# 新闻推送网站设计规范

> 版本：1.0 · 制定日期：2026-09-18  
> 风格名称：Quiet Brief / 安静的个性化新闻阅读器  
> 适用范围：兴趣订阅、每日简报、简报历史、新闻阅读、收藏、信息源、推送设置与 Agent 运行详情。  
> 文档状态：建议初版。当前未提供现有页面、品牌稿或代码，因此本文件是基于公开参考网站制定的新方案，不是对某个已有网站的逐像素还原或官方设计系统摘录。

## 0. 给设计者与 AI 编码工具的强制要求

**每次新增页面、修改页面、开发组件或开始新的前端会话，都必须先完整读取本文件，再查看项目中已有的共享组件与样式。**

本文件是本项目视觉与交互规范的统一依据。新增页面必须复用这里的颜色、字体、间距、布局与状态规则，不能因为页面不同而重新设计一套风格。实现技术不限定；可用于现有 HTML/CSS/JavaScript、React 或 Vue 前端，不要求为遵守规范更换项目框架。

| 要求 | 执行方式 |
| --- | --- |
| 优先复用 | 先检查已有 Button、Field、Dialog、NewsItem、PageHeader 等组件，再决定是否新增。 |
| 使用语义变量 | 颜色、圆角、阴影、常用间距应从全局设计变量读取；不得在各页面重复写独立色值。 |
| 不擅自改变整体风格 | 新增特殊样式必须有明确场景。涉及全局风格的变更，先更新本文件，再同步共享组件。 |
| 不牺牲业务语义 | 不得为统一外观而删掉来源、错误原因、时区、授权说明或关键操作。 |
| 必须实现状态 | 每个数据区都要设计加载、正常、为空、失败；交互控件补齐悬停、焦点、按下、禁用与提交中状态。 |
| 明确真实与演示 | 示例新闻、统计值、运行过程、推送结果必须标明演示；不得在接入真实数据后继续伪造这些信息。 |
| 保留可访问性 | 键盘操作、可见焦点、文字对比度、触屏操作和缩放可用性不是可删减的装饰。 |
| 新会话不可假装已读 | 文件不在工作区或无法读取时，说明缺失并请求提供；不得声称已经沿用未读取的规范。 |

本文件定义视觉与交互，不定义后端协议。下文的路由、状态名称与文案均为建议；与已有接口对接时，建立明确映射，不为迁就示例擅自修改后端业务。

---

## 1. 设计依据与参考网站

### 1.1 公开参考

调研日期为 2026-09-18。参考依据为公开网站及其官方产品展示；未对所有需要登录的应用页面进行完整实测。下表区分公开内容页与产品介绍页。色值和尺寸均由本规范独立制定，不声称是参考网站的实际设计参数。

| 参考 | 入口 | 观察对象与本项目的取舍 |
| --- | --- | --- |
| Feedly News Reader | `https://feedly.com/news-reader` | 官方介绍页含阅读器截图。借鉴侧栏分类、文章列表、来源管理；不复制企业情报产品的复杂面板。[R1] |
| Readwise Reader | `https://readwise.io/read` | 官方阅读产品介绍。借鉴专注阅读、阅读收件箱和收藏思路；不把批注等所有高级能力塞入首版。[R2] |
| Particle | `https://particle.news/` | 可直接浏览新闻列表与事件详情。借鉴事件聚合、摘要和关联报道入口；不采用强图片覆盖与高彩度背景作为默认阅读样式。[R3] |
| Inoreader | `https://www.inoreader.com/` | 官方介绍页含桌面界面。借鉴订阅分组、筛选与已读管理；不照搬多层工具导航。[R4] |
| TLDR | `https://tldr.tech/`、`https://tldr.tech/newsletters` | 可浏览新闻入口和主题订阅目录。借鉴短简报、清晰标题、来源与主题订阅入口。[R5] |
| 1440 | `https://join1440.com/`、`https://join1440.com/today` | 可查看每日简报及订阅入口。借鉴日期、栏目与连续阅读节奏；不将营销首页的大块展示直接用作登录后首页。[R6] |

**组合方式：Feedly 的信息组织 + Reader 的阅读取向 + TLDR 的简报节奏 + Particle 的来源意识。** 只借鉴必要结构，不混搭六套品牌视觉。

### 1.2 产品定位

这是一个“替用户筛选值得阅读的新闻”的个性化阅读网站，不是新闻门户、聊天机器人首页，也不是运营后台。

用户进入网站后，应很快理解三个问题：今天有什么值得读、为什么与我有关、下一次何时以及通过什么渠道收到简报。Agent 负责辅助完成工作，但阅读结果比 Agent 技术细节更重要。

风格关键词：**安静、清晰、可信、轻量、有编辑感。**

### 1.3 设计原则

- 内容优先：标题、摘要、来源的视觉优先级，高于封面图、标签和运行状态。
- 克制强调：暖白与中性色构成主体；深青绿只用于主操作、选中状态、链接与焦点。
- 信息渐进：列表展示摘要，详情展示依据与关联来源，运行日志按需展开。
- 阅读完整：正文允许自然纵向滚动，不为了“首屏放下所有内容”缩小字号或挤压行距。
- 状态真实：生成完成不等于推送成功，邮件提交不等于用户已收到，来源多不等于已核实。

### 1.4 禁止的视觉方向

禁止将霓虹渐变、玻璃拟态、大面积紫蓝色、发光边框、装饰性数据图、巨大 AI 欢迎语、连续弹窗作为默认风格。禁止所有区块都套厚阴影卡片、新闻列表卡片悬停上浮、标题贴在复杂照片上，以及用大量彩色徽章制造信息密度。

---

## 2. 配色规范

### 2.1 主色、辅助色与中性色

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| `--color-primary` | `#0F766E` | 深青绿；主按钮、链接、选中指示、键盘焦点。 |
| `--color-primary-hover` | `#115E59` | 主按钮与链接悬停。 |
| `--color-primary-active` | `#134E4A` | 主按钮按下。 |
| `--color-primary-soft` | `#E8F5F1` | 导航选中、关注主题选中、推荐理由的轻底色。 |
| `--color-primary-border` | `#B7DCD2` | 品牌浅色容器的装饰性边框，不单独承担交互边界。 |
| `--color-accent` | `#93611F` | 暖琥珀辅助色；只用于少量编辑性强调，不作为第二主按钮色。 |
| `--color-accent-soft` | `#FFF8E6` | 编辑性强调底色，不用于整页铺底。 |
| `--color-bg` | `#F7F8F5` | 页面暖白背景。 |
| `--color-surface` | `#FFFFFF` | 阅读内容、卡片、输入框、弹窗。 |
| `--color-surface-muted` | `#F1F3EF` | 次级容器、表头、骨架底色、悬停背景。 |
| `--color-text` | `#202721` | 标题与主要正文。 |
| `--color-text-secondary` | `#515C55` | 摘要、说明与次要正文。 |
| `--color-text-muted` | `#66716A` | 来源、时间、帮助文字、占位提示。 |
| `--color-text-inverse` | `#FFFFFF` | 深色实心按钮上的文字。 |
| `--color-text-disabled` | `#89938C` | 真正不可操作的禁用控件文字；不得用于正常元信息。 |
| `--color-disabled-bg` | `#EDF0EC` | 禁用控件背景。 |
| `--color-border` | `#DFE5DE` | 卡片、分隔线等装饰性边界。 |
| `--color-border-control` | `#7E8982` | 输入框、未选中复选框、次按钮等需要清晰辨认的控件边界。 |
| `--color-overlay` | `rgba(20, 28, 23, 0.38)` | 模态弹窗与移动端抽屉的遮罩。 |

浅色边框只用于分区，不得把 `--color-border` 当作输入框、开关未选中状态或键盘焦点的唯一提示。

### 2.2 语义状态色

| 状态 | 文字 / 图标 | 背景 | 装饰性边框 | 场景 |
| --- | --- | --- | --- | --- |
| 成功 | `#166534` | `#EDF7ED` | `#C5E4CA` | 已保存、已完成、已连接。 |
| 警告 | `#854D0E` | `#FFF8E6` | `#EDD79A` | 部分来源不可用、待验证、部分完成。 |
| 错误 | `#B42318` | `#FFF0ED` | `#F4C7BE` | 校验失败、投递失败、危险操作。 |
| 信息 | `#1D4ED8` | `#EEF4FF` | `#CAD9FA` | 说明性通知、系统提示。 |
| 处理中 | `#0F766E` | `#E8F5F1` | `#B7DCD2` | 排队之外的真实运行状态。 |
| 中性 | `#515C55` | `#F1F3EF` | `#DFE5DE` | 草稿、未启用、已取消、普通分类。 |

必须同时使用状态文字，必要时加图标。不能只用红绿点区分失败与成功。状态色不承担一般分类的装饰任务，不给每个主题随机分配一种颜色。

### 2.3 配色使用限制与检查

页面的视觉主体应为暖白、白色和灰绿色中性色。不要用主色铺满大面积内容容器。每个独立操作区域最多一个实心主按钮；其他操作使用描边、文字或菜单。

正文与正常说明文字以至少 4.5:1 的对比度作为本项目统一目标；用于识别控件的关键非文本边界与状态指示至少 3:1。前者比对大字号文字单独放宽更容易统一执行。[A1][A2]

以下为本文件给定纯色组合的计算值，使用 sRGB 相对亮度公式；并不代表所有页面和状态已经完成无障碍验收。

| 前景 / 背景 | 约对比度 |
| --- | --- |
| 主文字 `#202721` / 白色 `#FFFFFF` | 15.29:1 |
| 次级文字 `#515C55` / 页面底色 `#F7F8F5` | 6.54:1 |
| 弱化文字 `#66716A` / 页面底色 `#F7F8F5` | 4.76:1 |
| 弱化文字 `#66716A` / 次级背景 `#F1F3EF` | 4.55:1 |
| 白字 `#FFFFFF` / 主色 `#0F766E` | 5.47:1 |
| 控件边框 `#7E8982` / 白色 `#FFFFFF` | 3.63:1 |

不得给正常文字整体叠加低透明度后仍沿用以上结果。若改变底色、透明度或文字颜色，必须重新检查。首版只实现浅色主题；深色主题必须另行定义完整语义变量并验证，不使用整页反色或浏览器滤镜替代。

---

## 3. 字体与排版

### 3.1 字体家族

首版优先系统字体，不依赖在线字体加载成功才能正确布局。中文和英文统一使用无衬线体系，不为了“杂志感”给中文正文混用多种字体。

```css
--font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI",
  "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
  "Noto Sans CJK SC", sans-serif;
--font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas,
  "Liberation Mono", monospace;
```

正文、标题、按钮和导航使用 `--font-sans`。任务编号、技术参数和日志代码使用 `--font-mono`。时间、数量与耗时可使用 `font-variant-numeric: tabular-nums`，避免更新时跳动。

不在此文件中指定必须下载或分发的字体文件。后续引入品牌字体时需统一调整字体变量、核查授权并保留系统字体回退。

### 3.2 字号、字重与行高

下表 px 值以默认根字号 16px 为设计基准，实现优先使用 rem；不锁死用户的默认字号设置。

| 层级 | 桌面字号 / 行高 | 手机字号 / 行高 | 字重 | 用途 |
| --- | --- | --- | --- | --- |
| 营销页主标题 | 40 / 52px | 30 / 40px | 700 | 仅未登录介绍页，阅读首页不用。 |
| 简报详情标题 | 32 / 44px | 26 / 36px | 700 | 一期简报或长阅读页面标题。 |
| 页面标题 H1 | 28 / 36px | 24 / 32px | 700 | 今日简报、历史、订阅设置。 |
| 分区标题 H2 | 20 / 28px | 20 / 28px | 600 | 今日重点、主题分组、设置分区。 |
| 新闻列表标题 | 18 / 28px | 18 / 28px | 600 | 每条新闻标题。 |
| 阅读正文 | 18 / 32px | 17 / 30px | 400 | 简报详情的连续正文。 |
| 列表摘要 / 通用正文 | 16 / 26px | 16 / 26px | 400 | 摘要与页面说明。 |
| 导航 / 按钮 / 字段标签 | 14 / 20px | 14 / 20px | 500 | UI 操作与结构。 |
| 输入框文字 | 14 / 22px | 16 / 24px | 400 | 手机端提高输入可读性。 |
| 表格正文 | 14 / 22px | 14 / 22px | 400 | 历史与信息源管理。 |
| 时间 / 来源 / 帮助 / 错误 | 13 / 20px | 13 / 20px | 400 | 重要元信息仍需清晰可读。 |
| 非关键计数 / 紧凑徽章 | 12 / 18px | 12 / 18px | 500 | 只用于短标签或计数，不用于长句。 |

仅使用 400、500、600、700 四档常见字重。中文默认 `letter-spacing: normal`；不要套用英文超大标题的明显负字距。正文不做两端强制对齐，不使用全大写作为中文 UI 的装饰。

### 3.3 长内容与阅读节奏

阅读正文内容宽度上限 760px，推荐实际落在 680–760px；新闻流上限 900px。宽屏也不得把一段文字拉满整个屏幕。

正文段间距 16px；二级标题前 32px、后 16px；列表项之间 8px；来源区与正文之间 24px。列表摘要默认最多三行，新闻标题最多三行，详情页提供完整内容。表格标题可截为两行，但必须能通过标题链接进入完整详情，不以仅鼠标悬停的提示作为唯一补充。

中英文混排标题、长域名、无空格 URL 使用自然换行与 `overflow-wrap: anywhere`；普通中文正文不全局使用 `word-break: break-all`。新闻正文中的链接保留下划线或其他非颜色识别方式。

---

## 4. 间距、圆角、边框与层级

### 4.1 间距体系

采用 4px 基础网格。常用内外边距与 gap 从以下值选择，不因个人感觉反复出现 13px、19px、27px 等近似数值。字号、图标尺寸、控件高度和图片宽高比不受此限制。

| Token | 值 | 常见用途 |
| --- | --- | --- |
| `--space-1` | 4px | 图标与极短状态文字、帮助信息内部。 |
| `--space-2` | 8px | 图标与文字、同组按钮、小元素。 |
| `--space-3` | 12px | 标签间距、紧凑容器内边距。 |
| `--space-4` | 16px | 手机页边距、字段内部、段落间隔。 |
| `--space-5` | 20px | 新闻条目与普通卡片内边距。 |
| `--space-6` | 24px | 桌面卡片、表单字段组、主辅栏间距。 |
| `--space-8` | 32px | 页面标题与内容、桌面大屏页边距。 |
| `--space-10` | 40px | 阅读页较大分组间隔。 |
| `--space-12` | 48px | 页面级章节间距。 |
| `--space-16` | 64px | 营销页分区，不用于每条新闻。 |

同一语义关系固定同一间距：标签到输入框 8px，输入框到帮助/错误文字 4px，字段组之间 24px，设置区块之间 32px，页面标题到第一内容区 24px。

### 4.2 形状与边框

| 对象 | 圆角 | 边框 |
| --- | --- | --- |
| 按钮、输入框、菜单项、导航项 | 8px | 控件需要边界时为 1px 控件边框色。 |
| 普通卡片、新闻列表外容器 | 12px | 1px 装饰性边框色。 |
| 弹窗、抽屉上角、大浮层 | 16px | 1px 装饰性边框色。 |
| 普通状态徽章、代码片段底板 | 6px | 视状态使用语义边框或无边框。 |
| 主题标签 / 头像 | 999px / 50% | 主题标签可用胶囊形，常规主按钮不可全部胶囊化。 |

禁止在同一层级混用 8px、18px、24px 三套卡片圆角。分隔线统一为 1px，不通过 0.5px 技巧制造不可控差异。

### 4.3 阴影与叠层

| Token | 值 | 用途 |
| --- | --- | --- |
| `--shadow-none` | `none` | 新闻条目、卡片和普通按钮默认无阴影。 |
| `--shadow-popover` | `0 8px 24px rgba(20, 28, 23, 0.10), 0 2px 6px rgba(20, 28, 23, 0.04)` | 下拉、提示和通知浮层。 |
| `--shadow-dialog` | `0 24px 64px rgba(20, 28, 23, 0.18)` | 模态弹窗。 |

层级约定：普通内容 0，吸顶区 10，页面浮层 20，模态层 40，模态内部浮层 50，Toast 60。应使用统一 Portal/浮层容器管理堆叠；原生 `dialog` 的 top layer 需单独验证，不能只依赖 z-index。全屏模态打开时，不让背后的页面下拉菜单穿透到前面。

---

## 5. 页面布局与响应式

### 5.1 信息架构

| 页面 / 建议路由 | 职责 | 优先级 |
| --- | --- | --- |
| 今日简报 `/today` | 当前个性化简报、未读内容、生成入口。 | 首版 |
| 简报详情 `/briefs/:id` | 完整摘要、推荐理由、来源、生成与投递状态。 | 首版 |
| 历史简报 `/briefs` | 按日期、主题、状态检索过往简报。 | 首版 |
| 兴趣订阅 `/preferences` | 个人关注背景、主题、包含与排除关键词。 | 首版 |
| 推送设置 `/delivery` | 渠道、收件目的地、时间、时区与暂停。 | 首版 |
| 运行详情 `/runs/:id` | 真实 Agent 事件与问题排查。 | 首版，以辅助入口呈现 |
| 首次配置 `/onboarding` | 身份/关注背景、兴趣和可选推送配置。 | 首版，可简化为分区表单 |
| 收藏 `/saved` | 用户保留的新闻，支持主题与来源筛选。 | 后续增强 |
| 信息源 `/sources` | 用户信息源、启停、分组与抓取状态。 | 后续增强 |
| 设置聚合页 `/settings` | 手机端设置导航与账户入口。 | 随移动端实现 |

首版尚未实现的页面不在正式导航中放可点击的空链接；可以不显示，或以清楚标注的不可用入口呈现。不要求为完成视觉设计一次实现所有增强功能。

### 5.2 桌面主框架

左侧导航宽 240px；主区域顶部工具栏高 64px。主区域在扣除侧栏后布局，内容外容器宽度上限 1280px。新闻流本身宽度上限 900px，长阅读内容上限 760px。

1440px 桌面宽度默认使用“完整侧栏 + 居中新闻流”，不强行增加右栏。1536px 及以上可增加 280px 辅助栏，主辅栏间距 24px；辅助栏只放订阅摘要、下一次推送和当前任务，不放排行榜或装饰性图表。

页面建议结构：

```text
应用框架
├── 侧栏：产品标识 / 今日 / 历史 / 收藏 / 订阅设置 / 底部账户
└── 主区域
    ├── 顶栏：当前位置 / 搜索 / 通知 / 账户
    └── 页面内容
        ├── 页标题 + 更新信息 + 一个主要操作
        ├── 轻量筛选栏
        ├── 简报摘要或重要状态
        └── 新闻列表
            ├── 来源、发布时间
            ├── 新闻标题
            ├── 两至三行摘要
            ├── 推荐理由 / 相关报道入口
            └── 收藏、已读、更多
```

示意仅表达结构，不是需要原样渲染的文字。

### 5.3 断点规则

| 视口宽度 | 导航 | 内容布局 | 页边距 |
| --- | --- | --- | --- |
| `<768px` | 顶栏 56px；底部“今日、历史、收藏、设置”四项导航；未实现的项移除。 | 单列；右侧信息移入页面折叠区；弹窗适配手机。 | 16px |
| `768–1279px` | 72px 图标栏，按钮有可访问名称与悬停提示；可通过显式按钮展开完整导航。 | 单列正文；复杂筛选收进面板。 | 24px |
| `1280–1535px` | 完整 240px 侧栏。 | 单列主内容，不常驻右栏。 | 32px |
| `≥1536px` | 完整 240px 侧栏。 | 新闻页可加 280px 辅助栏；详情页仍限制阅读宽度。 | 32px |

断点仅控制布局。触控命中区以输入方式为准，不能假定大屏没有触屏。手机主操作控件最小 44×44px；紧凑桌面控件可为 36px 高，但不能小到无法识别或操作。WCAG 2.2 的目标尺寸最低条款以 24×24 CSS px 或规定例外为基础，44px 是本项目采用的更宽裕触屏目标，不把它误称为 AA 的统一最低值。[A3]

### 5.4 滚动与吸顶

主内容使用自然页面纵向滚动。不要给每张卡片独立滚动条。侧栏可独立滚动，长正文不锁死在固定高度窗口里。

吸顶顶栏使用不透明背景与底部分隔线。原则上同一页面最多叠加顶栏和一条必要的保存/筛选区域，不把日期、Tab、筛选、操作按钮全部分别吸顶。手机固定底部导航需保留 64px 加安全区的内容底部空间；有底部保存栏时将其放在导航上方并同步补足空间，避免覆盖正文或焦点。

新闻原文、日志与数据表允许在自己的容器内横向滚动，但页面本身不得整体横向滚动。固定宽度列必须使用 `min-width: 0`、可换行或响应式替代布局。

---

## 6. 通用组件规范

### 6.1 所有交互组件的共同状态

| 状态 | 统一规则 |
| --- | --- |
| 默认 | 边界、文字、可操作范围可辨认，不依赖悬停才出现关键入口。 |
| 悬停 | 仅调整文字、背景或边框；不改变组件尺寸和位置。触屏不依赖此状态。 |
| 焦点 | `:focus-visible` 使用 2px 主色轮廓与 3px 外偏移；不得移除焦点而不提供替代。 |
| 按下 | 使用更深色或更强底色；不做缩放弹跳。 |
| 禁用 | 使用专用禁用色、禁止触发，并在必要处说明原因；不以降低整个容器透明度代替。 |
| 加载 / 提交中 | 保持原按钮宽度和当前内容位置；提供动作文字，阻止重复提交。 |
| 错误 | 错误文案靠近问题，保留用户数据，给出重试或修改路径。 |

只对真的可点击对象使用指针光标。已经显示的新闻不因后台刷新变成全页骨架。

### 6.2 按钮 Button

标准高度 40px，左右内边距 16px，圆角 8px，文字 14px/20px、字重 500。紧凑按钮高 36px、左右 12px；强调按钮高 44px、左右 20px。触控模式统一至少 44px 命中高度。图标 16px，与文字间距 8px。

| 类型 | 外观 | 使用场景 |
| --- | --- | --- |
| Primary | 主色背景、白字、1px 主色边框、无阴影。 | 保存订阅、首次生成等当前区域唯一主操作。 |
| Secondary | 白底、主文字、控件边框；悬停次级底色。 | 查看历史、取消、预览。 |
| Ghost | 透明底、次级文字；悬停次级底色。 | 更多、筛选、次要工具栏动作。 |
| Link | 主色文字；正文位置需下划线。 | 查看原文、查看全部来源。 |
| Danger | 常态可用错误色文字；最终不可逆确认使用错误色实心。 | 删除订阅等明确危险动作。 |
| Icon | 40×40px 外框、18px 图标；触控 44×44px。 | 收藏、关闭、通知。 |

按钮使用具体动作：“保存订阅”“生成简报”“重试推送”，不使用含义不明的“确定”覆盖所有场景。加载态示例“正在保存…”；图标按钮必须有可访问名称。导航用链接，状态变更用按钮，禁止整页都用可点击 div。

### 6.3 卡片 Card 与分区 Panel

普通卡片白底、1px 分隔边框、12px 圆角，桌面内边距 24px、手机 16px。设置卡片之间 24px。新闻条目通常共享一个外容器，以细分隔线区分，不为每条新闻套三层卡片。

静态卡片不响应悬停；可点击卡片只改变浅背景或边框，不上浮、不放大。卡片内部有按钮时，避免将所有内容包在同一个链接或按钮中，防止嵌套交互。标题本身是明确的详情链接。

### 6.4 导航 Sidebar / Topbar / Bottom Navigation

侧栏使用页面底色或白底，右侧 1px 分隔线。产品标识区域高 64px，内边距 20px。导航项高 40px、圆角 8px、左右内边距 12px，项间距 4px；分组之间 24px。

选中项用主色浅底、主色文字、600 字重，可加 3px 指示条；设置 `aria-current="page"`。不只靠图标颜色表达选中。未读数字用低强调计数，不给所有入口挂红点。

顶栏仅保留导航、搜索、通知、账户等全局操作，不放大块欢迎文案。移动底栏高 64px 加安全区，图标 20px、标签 12px，选中状态可见且支持安全区；使用真实链接以支持返回与新标签页。

### 6.5 新闻条目 NewsItem

默认“标题 + 摘要”的列表视图，不默认瀑布流。条目左右内边距 20px、上下 20px；手机 16px。标题与来源之间 8px、标题与摘要之间 8px、摘要与底部操作之间 12px。

固定信息顺序：来源/发布时间 → 标题 → 摘要 → 推荐理由与来源数量 → 操作。标题 18px/28px、600；摘要 16px/26px、次级文字；来源时间 13px/20px。

推荐理由用一句自然语言，例如“与你关注的 Agent 工程化有关”。该示例不是模型准确率声明。不得仅显示“匹配度 98%”而不交代定义、数据来源和适用范围。

缩略图可选，桌面 112×84px、圆角 8px，手机可缩为 80×60px 或隐藏。图片缺失时优先纯文字布局，不自动生成装饰性假新闻图片。重要新闻若使用较大图，标题仍放在独立的纯色区域。图片采用已获准使用的资源；本设计规范不授予参考网站图片的使用权。

未读用较强标题字重及可辨识的“未读”标记；已读不把正文整体透明化。标记已读应基于明确操作或事先说明的产品规则，不因用户鼠标掠过就自动变更。

“查看原文”“收藏”“标记已读”不得互相误触。新标签打开外站时应有明确提示并采用安全的链接属性。收藏可乐观更新，但失败必须回滚并提示。

### 6.6 简报摘要 BriefSummary 与来源 SourceList

简报摘要作为页首紧凑内容块，内边距 24px、圆角 12px，白底或主色浅底，不使用巨大彩色 Banner。优先展示日期、新闻条数、关注主题与真实更新时间；只有有可靠计算时才展示“预计阅读 N 分钟”。

来源列表每条包含来源名称、可识别域名、文章标题、发布时间和原文链接。聚合事件展示“关联 N 篇报道”，不自动改写为“N 个独立来源”；多篇转载不应造成独立核实的假象。推荐理由与事实摘要在语义和版面上分开。

正文有来源引用时，使用文本编号或短来源标签；点击可定位到来源列表。失效来源显示具体失效状态，不能悄悄删除引用让用户无法追溯。避免未经依据的“权威认证”“事实已核实”徽章。

### 6.7 表格 Table

用于历史记录、信息源管理、投递记录，不用于主要阅读正文。

容器白底、1px 分隔边框、12px 圆角。表头底色为次级背景，最小高 44px；正文行最小高 56px，允许因内容变长自然增高。单元格左右 16px、上下 12px。表头 13px/20px、500，正文 14px/22px。默认仅横向分隔线，不画完整棋盘格，不默认使用斑马纹。

行悬停使用浅色底；选中行用主色浅底并配复选框，不只用颜色。数字右对齐、文字左对齐。操作列保留一个高频动作，其余放“更多”。排序使用标题按钮、图标和 `aria-sort`；筛选区与表头不要混淆。

手机优先转为标签明确的记录卡片；无法转换的数据表才局部横向滚动，并提供横向滚动提示。骨架行保留真实列宽；空状态占满表体，不保留一堆空白假行。加载失败必须给出重试，不能显示“暂无记录”。

### 6.8 弹窗 Dialog 与确认框

小确认框宽 400px，普通表单弹窗 560px，复杂预览 720px；最大宽度为视口减 32px。圆角 16px，白底，使用弹窗阴影与统一遮罩。内边距桌面 24px、手机 20px；标题 20px/28px、600；说明与内容相隔 16px；操作区与内容相隔 24px。

默认只允许一个模态层，不用“弹窗套弹窗”。复杂多分区设置应进入独立页面，不压进窄弹窗。长内容时弹窗最大高度为视口减 32px，内部内容可滚动，并保留可见的关闭和提交入口。

打开后焦点进入弹窗；Tab 焦点不逃出模态范围；Escape 可关闭或在有未保存修改时先进入明确的放弃确认；关闭后焦点返回触发元素。必须有可访问标题和独立关闭按钮，不能只靠点击遮罩关闭。[A4]

删除确认写清具体对象、影响和能否恢复。默认焦点优先放在安全操作或说明处，而不是危险按钮。仅“保存成功”不使用模态弹窗。

### 6.9 抽屉 Drawer

桌面右侧宽 480px，适合来源清单、筛选或简短运行详情。白底、边界清晰；阻断式抽屉使用模态行为，非阻断式侧栏不错误声明 `aria-modal`。

手机将长表单改为全屏抽屉；短筛选可为底部面板，顶部圆角 16px、高度不超过可视区域的 85%。保留标题、关闭按钮和保存入口，内容不被软键盘或底部安全区遮挡。完整简报阅读优先独立页面而不是长期停留在抽屉中。

### 6.10 下拉菜单 Dropdown / Popover / Select

操作菜单宽 200–280px，内边距 6px，圆角 12px、细边框、浮层阴影。菜单项最小高 36px，左右 12px，字 14px；触控项最小高 44px。分组以分隔线和小标题区分；危险动作放最后一组。

选择值的控件使用 Select/Combobox，执行动作的菜单使用 Menu，不能把全部实现为任意可点击列表。选中值有勾选与文字提示；键盘支持方向键、确认和退出，关闭后返回触发器。

超过约 8 个选项或内容难以扫描时提供搜索；IANA 时区列表使用可搜索选择器，不让用户滚动数百条。弹层自动避让视口，最大高度 320px，内部滚动。手机较复杂选择改成底部面板。

### 6.11 搜索与筛选

搜索输入包含真实标签或可访问名称，图标只是辅助。输入即时筛选时建议 300ms 防抖，同时保留 Enter 触发；这属于交互建议，不要求更改已有后端策略。提供清除输入按钮，清除不丢失其他筛选条件。

搜索结果显示作用范围，如“搜索历史简报”；不可把当前页过滤伪装为全站搜索。后台请求尚未结束时保留旧结果并显示更新中；输入新词后旧请求不得覆盖新结果。筛选项在 URL 或页面状态中保留，返回列表恢复筛选和滚动位置。

### 6.12 分页器 Pagination / 加载更多

历史与管理表格使用分页：默认每页 20 条，提供 20/50 条选择；数据范围、上一页、页码、下一页置于容器底部，距表体 16px。桌面页码按钮 36×36px、圆角 8px；触控为 44×44px。当前页用主色浅底和明显文字，不仅加深一个小数字。

手机可只保留“上一页 / 第 N 页 / 下一页”。后端未知总量时只显示已加载条数与上一页/下一页，禁止伪造总页数。切换主题或日期后回到第一页。

新闻流优先显式“加载更多”，不默认无限滚动。追加加载保留旧新闻，不抢走焦点，不重置阅读位置；返回列表应恢复已加载内容。

### 6.13 输入框与表单 Field / Input / Textarea

单行输入框标准高 40px，触控 44px，左右内边距 12px，圆角 8px，白底、1px 控件边框。多行输入框最小高 112px、内边距 12px，允许纵向调整或自适应增长。

标签放在上方，14px/20px、500，与输入框间距 8px。帮助与错误文字 13px/20px、间距 4px。必须有标签，不使用 placeholder 替代标签。必填以文字或清楚说明的标记表达；不要只依赖红色星号。

桌面表单容器最大宽 760px。大多数设置单列；只有相互关联、内容较短的字段可并列。手机全部单列。输入框与按钮在同行时必须对齐高度。

### 6.14 表单校验 Validation

| 场景 | 行为 |
| --- | --- |
| 首次输入前 | 不提前满屏报错。 |
| 字段失焦 | 校验已触达字段的格式和必要性。 |
| 点击提交 | 校验全部字段；定位并聚焦第一处错误。 |
| 错误后继续输入 | 合理实时清除已经修正的错误，不等再次提交。 |
| 服务端字段错误 | 映射回对应字段，保留已填数据。 |
| 服务端整体错误 | 表单顶部显示可重试的整体错误；不能只用转瞬即逝的 Toast。 |

输入框错误态使用错误色边框与文字，并设置 `aria-invalid`、通过 `aria-describedby` 关联具体说明。错误例：“请输入有效的邮箱地址”“请选择推送时区”；不要只写“输入错误”。

提交中只禁用会造成冲突的操作，不冻结整页。保存失败保留输入；有未保存修改离开页面时给出合理提醒。保存成功以明确结果反馈，不只更改按钮颜色。

### 6.15 复选框、单选框、开关与主题标签

复选框/单选框可视尺寸 18px，外层可点击标签区域至少达到当前输入方式的命中要求；未选中边界使用控件边框色。选择状态有勾选或圆点，不仅换底色。

开关轨道 40×24px，滑块 18px，标签清楚说明作用，如“启用每日推送”。开关自身外侧补足 44px 触控命中区。待保存的表单开关属于草稿，不得点击后立即产生外部副作用；立即生效的开关则显示提交过程并在失败后回滚。

主题标签高度可为 32px，左右 12px、圆角 999px、文字 14px。触控模式扩大可点击区域至 44px 并避免标签间命中区域重叠。已选主题使用主色浅底与勾选。包含关键词与排除关键词必须分区标注，不能只靠绿红色区分。

多选输入支持键盘添加、删除与去重；粘贴多个词后显示解析结果。界面同时说明匹配规则和允许数量，未定义规则时不要擅自声称支持复杂布尔表达式。

### 6.16 消息提示 Toast / Alert / Notification

Toast 用于“已收藏”“订阅已保存”等短时确认，桌面在右上角距边 24px，宽 320–400px，白底、12px 圆角、浮层阴影。图标 18px、标题 14px/20px、说明 13px/20px。最多同时显示 3 条，其余合并或排队。

普通信息约 5 秒后消失，鼠标悬停或键盘焦点停留时暂停计时；含必须处理的动作或重要错误时应持久显示或在页面保留等价入口。手机使用左右 16px 的单条提示，避开顶栏和底部导航。

业务错误、推送失败、授权失效用页面内 Alert，内边距 16px、8px 或 12px 圆角、语义浅底。消息必须有下一步，如“检查邮箱”“重试连接”，不只描述失败。

普通成功消息使用礼貌播报，不打断读屏阅读；紧急且确需立即感知的错误才使用强提醒。异步投递与任务结果同时进入持久通知或历史记录，不能只依赖页面打开时看到的 Toast。

### 6.17 空状态 EmptyState

结构为小图标 + 明确标题 + 原因/下一步 + 必要操作。普通空状态内边距 32–48px，文字区宽不超过 360px。图标不超过 40px，不使用巨幅插画占满阅读首页。

| 原因 | 示例标题 | 下一步 |
| --- | --- | --- |
| 尚未设置兴趣 | 先告诉我们你关注什么 | “设置订阅”。 |
| 还没有生成简报 | 第一份简报还未生成 | 显示已有订阅摘要与“生成简报”。 |
| 时间窗口内无匹配新闻 | 暂未找到符合条件的新闻 | 建议调整时间范围或订阅，不自动偷偷放宽规则。 |
| 当前筛选无结果 | 没有匹配的记录 | “清除筛选”，保留全部历史。 |
| 收藏为空 | 还没有收藏新闻 | 说明收藏入口，并可回到今日简报。 |
| 网络/服务失败 | 内容暂时无法加载 | 错误态与“重试”，不能用普通空状态掩盖。 |

### 6.18 步骤条 Stepper

仅用于确定顺序的首次配置，如“关注背景 → 兴趣主题 → 推送设置”，不用于假装固定顺序的 Agent 推理或工具调用。

桌面使用 28px 编号圆形与步骤标题，步骤之间连接线 1px；当前步骤主色描边/浅底、已完成勾选、未到达中性。手机用“第 2 步，共 3 步 + 当前标题”替代拥挤的横排。

支持返回前一步且保留输入。能否跳过在当前步骤明确说明；仅站内阅读的用户可跳过外部推送设置，不强制绑定尚未同意的渠道。

### 6.19 进度条 Progress 与加载 Skeleton

确定总量的任务可使用 6px 高进度条，轨道为次级背景、填充为主色、圆角 999px。旁边展示如“已处理 6/10 条”，并提供相应可访问数值。

无法确定总量的 Agent 任务只展示当前状态、已耗时和实际事件；可使用不定进度指示，但不显示伪造的 87% 或“还剩 5 秒”。流程预算上限不是完成度分母，不能用“已使用工具调用次数 / 最大次数”冒充生成进度。

骨架与真实布局同尺寸，使用中性色块；默认静态或轻微透明变化，不使用高对比扫光。已有内容后台更新时保留内容并显示小型状态，避免重排。超过预期等待应显示真实的延迟/连接状态及可执行下一步，不能一直没有说明地转圈。

### 6.20 徽章 Badge / 计数 / 主题标签

状态徽章最小高 24px，左右 8px，6px 圆角，12px/18px、500，使用对应语义文字与浅底。允许图标 12px，但状态名不可省略。

主题标签表示“内容属于什么”，状态徽章表示“任务处于什么状态”，不能混用。单条新闻同时可见的非必要标签原则上不超过两个，其余收进详情。计数上限可显示 99+，完整数量在可访问名称或详情中提供。

### 6.21 Tooltip / Tabs / Accordion

Tooltip 用于解释图标，不承载必读错误或关键操作。浅色或深色方案二选一并全局统一；本项目默认白底、正文色、8px 圆角、8px 内边距、浮层阴影，文字 12px/18px，最大宽 240px。悬停约 300ms 或键盘焦点后展示，支持 Escape 关闭，不只能用鼠标触发。

同页筛选采用 40px 高 Tab，主色文字与 2px 下划线表达当前项；简单数据筛选也可用分段按钮，但不可和真正的页面导航混为一谈。多标签横向溢出时局部滚动，不把标签缩成无法阅读的字号。

Accordion 标题区域最小高 44px，左右 16px，展示展开图标与 `aria-expanded`，适合来源详情、推送高级设置和 Agent 参数摘要。展开区域内边距 16px，内容变化不自动把页面滚回顶部。

---

## 7. 新闻订阅与推送的专用交互

### 7.1 兴趣订阅页

表单按照“关注背景、主题、包含关键词、排除关键词、简报偏好”分区。关注背景使用明确文案，例如“你的角色或关注方向”，与账户实名/登录身份区分，不误导用户必须提交敏感身份信息。

初始主题以少量建议供选择，不默认全选。关键词与排除词分成独立输入区，并解释实际匹配含义。新闻时间窗口、最大条数、语言作为简报偏好，不混入账户设置。

桌面可在右侧提供只读“订阅摘要”，手机放在保存操作之前。摘要只重述用户真实配置，不生成虚假的新闻预览或匹配数量。页面采用显式保存，保存后清楚说明何时生效，不在每次输入时立刻触发 Agent 任务。

### 7.2 推送设置页

渠道采用分区行或简洁卡片：“站内通知”“电子邮件”等。只展示后端真实支持的渠道，未接入的渠道不伪装为可用。启用外部渠道前说明目的地、频率与验证方式，并由用户明确操作。

时间字段必须与时区并列显示；保存后展示后端确认的下一次运行时间，例如带日期、时间和时区的完整结果，而不是仅写“明早 8 点”。支持夏令时的时区应说明以当地时间调度的实际策略；显示由服务端计算的结果，不自行用浏览器时区覆盖用户设置。

“保存设置”“发送测试”“暂停推送”是三个不同动作。测试推送需要明确点击；暂停不会删除历史或取消收藏。恢复时说明新的下一次调度，不默认补发全部遗漏简报。

收件地址在列表/摘要中适当脱敏，编辑时允许用户确认完整目的地。不要将 API Key、Bearer Token、SMTP 密码或其他密钥出现在截图、运行事件、前端日志和 URL 中。

### 7.3 状态必须分开

下表为 UI 语义建议，具体后端枚举需做映射。

| 维度 | 建议状态 | 展示原则 |
| --- | --- | --- |
| 简报生成 | 排队中、生成中、已生成、部分完成、失败、已取消 | 与推送结果分开；“部分完成”能查看可用内容和缺失原因。 |
| 外部推送 | 未启用、待提交、已提交发送、已送达、失败、状态未知 | 只有真实回执支持时才能显示“已送达”；服务接受请求只显示“已提交发送”。 |
| 新闻阅读 | 未读、已读、已收藏 | 收藏不是已读；用户可以撤销自己的标记。 |
| 信息源 | 正常、同步中、暂不可用、待验证、已停用 | 不将单个来源问题伪装成整个简报失败。 |

“重新生成”产生新内容版本；“重试推送”再次尝试投递同一份内容，两者必须分开操作和记录。历史详情应能够识别当前展示的简报版本及对应投递记录。

---

## 8. Agent 运行可视化

### 8.1 普通阅读页中的呈现

默认使用紧凑状态行，例如“正在整理与你关注主题有关的新闻 · 查看过程”。位置在简报标题附近或辅助区，不出现满屏终端、聊天气泡或复杂流程图。

已有简报在新任务运行时仍可阅读。页面关闭、网络重连和任务失败不会让历史内容消失。任务创建成功后，页面使用后端任务标识跟踪，不因反复点击重复创建任务。

### 8.2 运行详情页

以真实事件时间线或可折叠列表呈现。每条事件展示时间、动作名称、状态、耗时与简短结果。详情中按授权展示脱敏后的工具参数与结果摘要，可展示工具调用轮次、已完成工作量和实际 usage 数据。

时间线是事件回放，不是预先写死的“搜索 → 抓取 → 总结 → 完成”流程。工具可以重复调用或调整顺序；前端不伪造尚未发生的步骤。

不展示模型私有思考过程，不把系统提示词、访问凭证或其他用户的数据作为“透明度”内容。面向普通用户的摘要写“读取了 3 个来源”，技术面板可进一步展示对应真实工具名称，两层信息不混为一谈。

### 8.3 事件与故障状态

| 情况 | 交互 |
| --- | --- |
| 新事件到达 | 稳定追加，使用事件 ID 去重，不重复闪烁旧内容。 |
| 用户正查看旧事件 | 不强制滚到底部；显示“有新事件”按钮。 |
| 流式连接断开 | 显示“连接暂时中断，任务状态待确认”；不能直接判定任务已失败。 |
| 重连成功 | 恢复最新状态和遗漏事件，不重新开始假进度。 |
| 单个工具失败后继续 | 显示该事件失败，但整体任务状态仍由后端决定。 |
| 达到预算或部分来源失败 | 说明真实限制；有结果时允许查看，并清楚标明缺失。 |
| 用户请求取消 | 先显示“正在取消”；后端确认后再显示“已取消”。 |

日志默认浅色底与等宽字体，单行长参数在局部容器内滚动，默认折叠大 JSON。不要让技术参数撑宽整个网页。

---

## 9. 关键页面模板

### 9.1 今日简报

首屏从“今日简报”开始，不从营销口号、四张统计卡或聊天输入框开始。标题旁显示真实更新状态，下方显示关注主题、当前筛选和内容。

有内容时展示一段简短概览与新闻列表；生成中展示紧凑运行状态并保留已有内容；无订阅、无内容、无匹配、加载失败采用各自的状态设计。页面不同时出现“生成”“刷新”“更新”三个含义接近的主操作。

### 9.2 简报详情

顶部为返回列表、日期/版本、标题与来源说明。正文单栏阅读宽度不超过 760px，依次为简报概览、主题分区、每条摘要与推荐理由、来源区域。收藏与阅读操作不遮挡正文。

生成过程和推送记录默认折叠或通过轻量入口进入，不在每条新闻后插入完整运行日志。来源与引用必须可查；AI 摘要不冒充媒体原文。历史内容生成时使用的偏好快照与当前偏好不同，应能解释差异。

### 9.3 历史简报

页标题下提供日期范围、主题、生成/推送状态筛选。桌面采用表格或整齐列表，核心字段为日期、简报标题、条数、生成状态、推送状态、查看详情。不要把大量无用户价值的任务 ID 放在主列。

手机使用记录卡片，字段有标签，区分“内容已生成”和“推送失败”。从详情返回时恢复筛选、分页和滚动位置。

### 9.4 信息源与收藏

信息源用清晰来源名、域名、分组、状态和启停操作。新增来源表单显示解析/验证状态，失败时保留输入。来源列表中的可见开关语义明确，不把“停止订阅”混同“删除历史”。

收藏复用 NewsItem，不再开发一套不同的卡片；增加收藏时间或主题筛选即可。删除收藏提供短时撤销或可重新收藏的明确入口，不使用危险删除级别的打断性确认。

### 9.5 未登录介绍页

需要营销页时保持相同字体与配色：一个价值主张、一段解释、一个开始订阅按钮、一份明确标注的示例简报。可以比应用内使用更宽松间距，但不把首版做成多屏动画展示站。

示例内容、用户数量、来源数量、好评与节省时间等必须有真实依据或明确标为示例；不能为了让页面看起来成熟编造数据。

---

## 10. 动效、图标与文案

### 10.1 动效

微交互 120ms，菜单/背景切换 180ms，弹窗/抽屉 220ms；默认缓动 `cubic-bezier(0.2, 0, 0, 1)`。只动画 opacity、少量 transform 或必要尺寸，不做大幅滚动视差和循环装饰动效。

新闻条目不悬停上浮，页面跳转不逐条延迟弹入几十条新闻。遵守 `prefers-reduced-motion`：去除非必要过渡与自动平滑滚动，加载反馈仍通过文字和状态保留。计时器以真实时间更新，不制造“AI 正在思考”的打字动画。

### 10.2 图标

采用项目已有的一套线性图标，统一 1.75–2px 视觉线宽；常规 16px、导航 20px、空状态不超过 40px。不为同一产品混用多套不同风格的图标。功能按钮不使用 emoji 替代正式图标。

装饰图标对辅助技术隐藏，有独立语义的图标提供名称。来源 logo 不存在时可用来源名称首字母或中性站点图标，不用与真实品牌易混淆的生成图。

### 10.3 文案

保持直接、平静、具体。推荐“已保存，下次生成将使用新订阅”“已生成 8 条新闻，邮件发送失败，可重试”；避免“AI 魔法正在发生”“解锁无限资讯力量”等空泛话术。

日期遵循用户选择的时区。列表可显示相对时间，详情提供可见或可访问的绝对时间；新闻发布时间、简报生成时间、投递时间不能混为一个“更新时间”。

---

## 11. 无障碍与内容稳定性

以 WCAG 2.2 AA 的相关要求作为实现检查依据，但不得仅凭此设计文件宣称整个网站已合规。[A1][A2][A3]

页面采用语义结构：导航、主内容、标题、文章、表单和表格使用对应元素。每页有清楚的主标题，标题层级不为追求字号随意跳跃。提供跳转主内容的键盘入口。

所有关键流程应只用键盘完成，包括订阅设置、查看简报、选择筛选项、打开/关闭弹窗与重试推送。不会移到不可见区域的焦点优先于装饰性吸顶效果。手机与 200% 文字缩放下表单标签、按钮和错误提示不截断。

在相当于 320 CSS px 的窄视口测试阅读和设置重排。长域名、长关键词、无图新闻、100 字以上标题、多个错误提示、空结果与大量事件都要有明确布局，不用正常演示数据的长度作为唯一验收依据。

图片预留尺寸以避免加载时推挤文字；非首屏图片可延迟加载。外部资源加载失败不影响标题、正文和来源链接。较长数据列表按需分页或采用经验证的虚拟化方案，不为追求速度破坏键盘与读屏体验。

---

## 12. 可直接使用的 CSS 设计变量与基础样式

本节是设计变量的代码落点。可复制到现有全局样式文件，不要求另起前端项目。纯色值应尽量只在此变量区域出现；组件通过语义变量引用。文档表格与代码同时变更，避免两个版本漂移。

```css
:root {
  color-scheme: light;

  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI",
    "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
    "Noto Sans CJK SC", sans-serif;
  --font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas,
    "Liberation Mono", monospace;

  --color-primary: #0F766E;
  --color-primary-hover: #115E59;
  --color-primary-active: #134E4A;
  --color-primary-soft: #E8F5F1;
  --color-primary-border: #B7DCD2;
  --color-accent: #93611F;
  --color-accent-soft: #FFF8E6;

  --color-bg: #F7F8F5;
  --color-surface: #FFFFFF;
  --color-surface-muted: #F1F3EF;
  --color-text: #202721;
  --color-text-secondary: #515C55;
  --color-text-muted: #66716A;
  --color-text-inverse: #FFFFFF;
  --color-text-disabled: #89938C;
  --color-disabled-bg: #EDF0EC;
  --color-border: #DFE5DE;
  --color-border-control: #7E8982;
  --color-overlay: rgba(20, 28, 23, 0.38);

  --color-success: #166534;
  --color-success-bg: #EDF7ED;
  --color-success-border: #C5E4CA;
  --color-warning: #854D0E;
  --color-warning-bg: #FFF8E6;
  --color-warning-border: #EDD79A;
  --color-danger: #B42318;
  --color-danger-bg: #FFF0ED;
  --color-danger-border: #F4C7BE;
  --color-info: #1D4ED8;
  --color-info-bg: #EEF4FF;
  --color-info-border: #CAD9FA;

  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-5: 1.25rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-10: 2.5rem;
  --space-12: 3rem;
  --space-16: 4rem;

  --radius-sm: 0.375rem;
  --radius-control: 0.5rem;
  --radius-card: 0.75rem;
  --radius-dialog: 1rem;
  --radius-pill: 999px;
  --border-width: 1px;

  --shadow-none: none;
  --shadow-popover: 0 8px 24px rgba(20, 28, 23, 0.10),
    0 2px 6px rgba(20, 28, 23, 0.04);
  --shadow-dialog: 0 24px 64px rgba(20, 28, 23, 0.18);

  --font-size-xs: 0.75rem;
  --font-size-meta: 0.8125rem;
  --font-size-ui: 0.875rem;
  --font-size-body: 1rem;
  --font-size-news-title: 1.125rem;
  --font-size-section: 1.25rem;
  --font-size-page-title: 1.75rem;
  --font-size-article-title: 2rem;
  --font-size-reading: 1.125rem;
  --line-height-body: 1.625;
  --line-height-reading: 1.7777778;

  --control-height-sm: 2.25rem;
  --control-height: 2.5rem;
  --control-height-lg: 2.75rem;
  --touch-target: 2.75rem;
  --topbar-height: 4rem;
  --sidebar-width: 15rem;
  --sidebar-rail-width: 4.5rem;
  --support-width: 17.5rem;
  --width-page: 80rem;
  --width-feed: 56.25rem;
  --width-reading: 47.5rem;
  --width-form: 47.5rem;
  --page-padding: var(--space-6);

  --z-sticky: 10;
  --z-popover: 20;
  --z-modal: 40;
  --z-modal-popover: 50;
  --z-toast: 60;
  --duration-fast: 120ms;
  --duration-normal: 180ms;
  --duration-slow: 220ms;
  --ease-standard: cubic-bezier(0.2, 0, 0, 1);
}

* { box-sizing: border-box; }
html { font-size: 100%; }
body {
  margin: 0;
  min-width: 0;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-sans);
  font-size: var(--font-size-body);
  line-height: var(--line-height-body);
}
button, input, select, textarea { font: inherit; }
img { max-width: 100%; height: auto; }
a { color: var(--color-primary); }
a:hover { color: var(--color-primary-hover); }
:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 3px;
}
::selection {
  color: var(--color-text);
  background: var(--color-primary-soft);
}

.page-container {
  width: 100%;
  max-width: var(--width-page);
  min-width: 0;
  margin-inline: auto;
  padding: var(--space-6) var(--page-padding);
}
.feed-container {
  width: 100%;
  max-width: var(--width-feed);
  min-width: 0;
  margin-inline: auto;
}
.reading-container {
  width: 100%;
  max-width: var(--width-reading);
  min-width: 0;
  margin-inline: auto;
  font-size: var(--font-size-reading);
  line-height: var(--line-height-reading);
  overflow-wrap: anywhere;
}
.reading-container p { margin-block: 0 var(--space-4); }
.reading-container a {
  text-decoration: underline;
  text-underline-offset: 0.18em;
}
.card {
  min-width: 0;
  padding: var(--space-6);
  background: var(--color-surface);
  border: var(--border-width) solid var(--color-border);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-none);
}
.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  min-height: var(--control-height);
  padding: var(--space-2) var(--space-4);
  border: var(--border-width) solid var(--color-border-control);
  border-radius: var(--radius-control);
  background: var(--color-surface);
  color: var(--color-text);
  font-size: var(--font-size-ui);
  font-weight: 500;
  line-height: 1.4285714;
  text-decoration: none;
  cursor: pointer;
  transition: background-color var(--duration-fast) var(--ease-standard),
    border-color var(--duration-fast) var(--ease-standard),
    color var(--duration-fast) var(--ease-standard);
}
.button:hover { background: var(--color-surface-muted); }
.button--primary {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: var(--color-text-inverse);
}
.button--primary:hover {
  background: var(--color-primary-hover);
  border-color: var(--color-primary-hover);
  color: var(--color-text-inverse);
}
.button--primary:active {
  background: var(--color-primary-active);
  border-color: var(--color-primary-active);
}
.button:disabled,
.button[aria-disabled="true"] {
  background: var(--color-disabled-bg);
  border-color: var(--color-border);
  color: var(--color-text-disabled);
  cursor: not-allowed;
}
/* aria-disabled 本身不会阻止事件；使用它的组件必须在逻辑层阻止激活。 */
.field-control {
  width: 100%;
  min-width: 0;
  min-height: var(--control-height);
  padding: var(--space-2) var(--space-3);
  border: var(--border-width) solid var(--color-border-control);
  border-radius: var(--radius-control);
  background: var(--color-surface);
  color: var(--color-text);
  font-size: var(--font-size-ui);
  line-height: 1.5714286;
}
.field-control::placeholder { color: var(--color-text-muted); opacity: 1; }
.field-control[aria-invalid="true"] { border-color: var(--color-danger); }
.field-control:disabled {
  background: var(--color-disabled-bg);
  color: var(--color-text-disabled);
}
.meta {
  color: var(--color-text-muted);
  font-size: var(--font-size-meta);
  line-height: 1.5384615;
}
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip-path: inset(50%);
  white-space: nowrap;
  border: 0;
}

@media (min-width: 1280px) {
  :root { --page-padding: var(--space-8); }
}
@media (max-width: 767px) {
  :root {
    --page-padding: var(--space-4);
    --topbar-height: 3.5rem;
    --font-size-page-title: 1.5rem;
    --font-size-article-title: 1.625rem;
    --font-size-reading: 1.0625rem;
    --line-height-reading: 1.7647059;
    --control-height: 2.75rem;
  }
  .card { padding: var(--space-4); }
  .field-control { font-size: 1rem; line-height: 1.5; }
}
@media (any-pointer: coarse) {
  .button, .field-control { min-height: var(--touch-target); }
  .icon-button {
    min-width: var(--touch-target);
    min-height: var(--touch-target);
  }
}
@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  .button { transition: none; }
  .skeleton, .decorative-animation { animation: none; }
  /* 其他新组件必须在自己的动效规则中同步适配该偏好。 */
}
```

上述样式只是公共基础，不是完整组件库。模态焦点管理、下拉键盘行为、提交防重、权限控制和 SSE 恢复仍需在组件逻辑中实现；不能仅复制 CSS 就宣称交互完成。

---

## 13. 组件复用与开发流程

建议优先沉淀以下三类组件，不要求采用固定目录或文件扩展名：

| 层级 | 组件 |
| --- | --- |
| 基础 UI | Button、IconButton、Field、Input、Select、Checkbox、Switch、Badge、Dialog、Drawer、Toast、Alert、EmptyState、Skeleton、Pagination。 |
| 页面结构 | AppShell、Sidebar、Topbar、PageHeader、FilterBar、FormSection、ReadingLayout。 |
| 新闻业务 | NewsItem、BriefSummary、SourceList、TopicSelector、DeliveryStatus、RunStatus、RunTimeline。 |

开发一个新页面时：先确认任务涉及的页面模板与组件；读取现有实现；补充缺失的共享能力；再组装页面；最后检查真实数据、长内容、异常状态、窄屏和键盘操作。不要先造一个独立页面，再用几十条覆盖样式勉强贴近规范。

页面级 CSS 负责布局，基础组件 CSS 负责控件外观。不要用 `nth-child` 给某个按钮强行改颜色，也不要依赖新闻条目恰好有两行文字来对齐操作。设计变量修改应回归所有受影响组件。

新页面实现完成时，交付说明至少明确：复用了哪些组件、覆盖了哪些状态、在什么尺寸验证过、哪些依赖真实后端尚未验证。不得把静态演示点击效果描述为已经实现真实推送或 Agent 执行。

---

## 14. 验收清单

### 14.1 视觉与响应式

- [ ] 页面使用统一暖白底与深青绿主色，没有新的任意配色、阴影或卡片圆角。
- [ ] 标题、摘要、来源、操作的层级清楚；首屏以新闻为主，不被技术状态或统计卡占满。
- [ ] 字号、行高与 4px 间距体系符合规范，正文不靠过浅颜色降低存在感。
- [ ] 1440px 下不强塞三栏；宽屏阅读内容没有无限拉宽。
- [ ] 320、375、768、1024、1440、1920 CSS px 下无页面级横向溢出。
- [ ] 手机安全区、软键盘与固定导航不遮住提交按钮、错误提示和焦点。

### 14.2 组件与状态

- [ ] 按钮、卡片、导航、表格、弹窗、下拉、分页、表单、消息、空状态、步骤条、进度条与徽章复用统一组件。
- [ ] 所有数据区区分加载、为空、筛选无结果、失败和成功。
- [ ] 提交中不重复触发；保存失败保留输入；成功有清楚反馈。
- [ ] 断网时不把任务状态误写为失败；后台刷新不清空已经展示的简报。
- [ ] 已生成、已提交发送、已送达、已读、已收藏分别展示，不混用。

### 14.3 阅读、可信度与可访问性

- [ ] 每条新闻可追溯来源；聚合报道数量不冒充独立核实数量。
- [ ] 推荐理由与新闻事实分开；示例数据明确标记，不伪造准确率、热度或进度。
- [ ] Agent 时间线来自真实事件，可重复调用工具，不显示私有思考或敏感参数。
- [ ] 时间设置显示时区和真实下次运行时间，测试发送由用户明确触发。
- [ ] 所有关键流程可以只用键盘完成，弹窗关闭后焦点返回正确位置。
- [ ] 对比度、触控命中区、长标题、缩放与减少动效偏好已检查。

---

## 15. 写入项目指令文件的规则

将下面内容合并到项目现有的 `AGENTS.md` 或团队约定的等价指令文件，不要覆盖其中已有的安全与开发要求。Codex 会在启动时按目录层级读取相应的 AGENTS 指令；不要把本文件全文塞进 AGENTS.md，而应在其中保留“先读取 design.md”的短规则，完整规范继续作为独立文件。[T1] 指令文件的自动读取行为取决于具体编码工具；普通聊天新会话仍需提供可访问的项目文件。

```markdown
## 前端视觉与交互规范

在新增或修改任何前端页面、组件和样式之前，先完整读取项目根目录
`design.md`，再查看现有共享组件及全局样式。

严格沿用其中的配色、字体、字号、行高、间距、圆角、阴影、响应式布局
和组件状态规范。优先复用已有组件，不为单个页面创建独立视觉体系。

新增页面必须覆盖正常、加载、空状态、失败和关键交互状态；保证来源
可追溯、任务状态真实、移动端与键盘操作可用。不得伪造新闻、统计值、
Agent 进度或推送成功状态。

需要改变全局设计时，先更新 design.md，再同步共享样式与相关组件。
如果无法读取 design.md，明确说明缺失，不得假装已经遵守。
```

## 16. 参考来源与适用边界

这些来源用于观察产品结构或核对标准要求，不表示相关产品认可本方案。不要直接复制它们的 logo、付费素材或整套品牌外观。

- [R1] Feedly，News Reader 官方介绍与界面展示：`https://feedly.com/news-reader`
- [R2] Readwise，Reader 官方介绍：`https://readwise.io/read`
- [R3] Particle，公开新闻列表和可从列表进入的新闻详情：`https://particle.news/`
- [R4] Inoreader，官方介绍与桌面界面展示：`https://www.inoreader.com/`
- [R5] TLDR，公开新闻入口和订阅目录：`https://tldr.tech/`；`https://tldr.tech/newsletters`
- [R6] 1440，官网和每日简报入口：`https://join1440.com/`；`https://join1440.com/today`
- [A1] W3C，Understanding SC 1.4.3: Contrast (Minimum)：`https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html`
- [A2] W3C，Understanding SC 1.4.11: Non-text Contrast：`https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html`
- [A3] W3C，Understanding SC 2.5.8: Target Size (Minimum)：`https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html`
- [A4] W3C WAI-ARIA APG，Dialog (Modal) Pattern：`https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/`

- [T1] OpenAI，Custom instructions with AGENTS.md：`https://developers.openai.com/codex/guides/agents-md/`

### 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 1.0 | 2026-09-18 | 制定阅读器式浅色视觉、页面模板、组件细则、订阅/推送语义、Agent 可视化、CSS 变量与验收要求。 |
