# 知更管理员端界面设计规范

> 版本：2.2 · 日期：2026-09-18 · 对应原型：ITERATE r5
>
> 风格：安静、清晰、可追溯的运行控制台
>
> 适用范围：仅管理员端 `admin-web/` 的概览、采集来源、模型、Agent 配置、运行监测、评估、用户与投递管理。用户端遵循 [design-user.md](design-user.md)。

## 1. 适用规则与设计方向

新增或修改管理员页面、组件与样式前，完整阅读本文件，再检查现有共享组件和全局样式。复用深青绿、暖白、系统字体、侧栏框架和管理表格，不为单个页面创建独立视觉体系。

本文件由原统一规范收敛为管理员控制台规范，保留原型 r1 的管理员视觉。用户提供的新参考图只用于用户端，不改变本端侧栏、设计变量和运行监测的信息密度。

核心任务是辨认异常、修改配置、追踪执行及比较效果。界面优先展示状态、对象、配置版本、时间和可执行下一步。信息渐进展开，运行事件、参数差异和样例明细按需查看。禁止霓虹渐变、玻璃拟态、发光边框、大面积紫蓝、巨大 AI 欢迎语与装饰性统计图。

每个数据区覆盖正常、加载、为空、失败、部分完成和关键交互。来源、错误原因、时区与权限信息不能为视觉简洁被删除。原型数据和模拟执行持续标明；不把前端模拟当成真实模型、采集、沙箱或发送能力。

## 2. 配色与语义

| 变量 | 值 | 用途 |
| --- | --- | --- |
| `--color-primary` | `#0F766E` | 主操作、链接、选中、焦点。 |
| `--color-primary-hover` / `--color-primary-active` | `#115E59` / `#134E4A` | 悬停与按下。 |
| `--color-primary-soft` / `--color-primary-border` | `#E8F5F1` / `#B7DCD2` | 选中浅底与装饰边框。 |
| `--color-bg` / `--color-surface` | `#F7F8F5` / `#FFFFFF` | 暖白页面与白色面板。 |
| `--color-surface-muted` | `#F1F3EF` | 表头、次级容器、悬停。 |
| `--color-text` / `--color-text-secondary` / `--color-text-muted` | `#202721` / `#515C55` / `#66716A` | 标题正文、说明、元信息。 |
| `--color-border` / `--color-border-control` | `#DFE5DE` / `#7E8982` | 分区与可操作控件边界。 |
| `--color-accent` / `--color-accent-soft` | `#93611F` / `#FFF8E6` | 少量辅助强调。 |

成功使用 `#166534` / `#EDF7ED`，警告 `#854D0E` / `#FFF8E6`，错误 `#B42318` / `#FFF0ED`，信息 `#1D4ED8` / `#EEF4FF`。每个状态有文字，必要时补图标，不能仅凭红绿点区分。

装饰浅边框不作为输入框或焦点的唯一边界。每个独立操作区域最多一个实心主按钮；常规分类不随机分配高饱和颜色。正文及说明以 4.5:1、关键控件边界以 3:1 为检查目标；变化后的组合需重新核对。

## 3. 字体与排版

沿用系统无衬线 `-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif`。模型 ID、run ID、参数、日志与代码使用系统等宽字体；时间和数量使用等宽数字。

| 对象 | 字号 / 行高 | 字重 |
| --- | --- | --- |
| 页面标题 | 28 / 36px；手机 24 / 32px | 700 |
| 分区标题 | 20 / 28px | 600 |
| 正文说明 | 16 / 26px | 400 |
| 表格正文 | 14 / 22px | 400 |
| 导航、按钮、标签 | 14 / 20px | 500 |
| 时间、帮助、错误 | 13 / 20px | 400 |
| 短状态徽章 | 12 / 18px | 500 |

字号实现优先 rem，根字号不锁死。中文不做负字距与强制两端对齐。长 URL 和 ID 使用 `overflow-wrap: anywhere`；日志参数局部滚动，不能撑宽整页。表格可截断非关键长描述，但有可键盘进入的明细；不能只靠鼠标悬停补全。

## 4. 间距、形状与层级

4px 间距网格：4、8、12、16、20、24、32、40、48、64px。字段标签到控件 8px、控件到帮助 4px、字段组 24px、设置区块 32px。桌面面板内边距 24px，手机 16px。

控件圆角 8px，普通面板 12px，弹窗 16px，徽章 6px。控件边框与分隔线均为 1px。普通面板、表格与按钮默认无阴影；仅菜单和弹窗使用第 12 节统一阴影。

层级为普通内容 0、吸顶 10、浮层 20、模态 40、模态内部浮层 50、Toast 60。原生 dialog 的 top layer 单独验证，不靠任意增加 z-index 解决焦点问题。

## 5. 管理框架与响应式

桌面固定导航宽 240px，顶栏高 64px，主区域扣除侧栏后布局，内容最大宽 1280px。导航顺序为概览、新闻来源、模型配置、Agent 配置、运行记录、评估实验、用户、投递记录。产品标识及演示标记清楚；全局场景入口放顶栏，不混入业务配置。

| 宽度 | 导航与内容 |
| --- | --- |
| `<768px` | 56px 顶栏与显式菜单入口；主要内容单列，完整导航放可关闭的菜单/抽屉。表单全单列，手机保留 16px 页边距。 |
| `768–1279px` | 72px 图标导航栏，可访问名称及展开入口；内容 24px 页边距。 |
| `≥1280px` | 完整 240px 侧栏，主内容 32px 页边距；表格、配置双栏和明细按任务需要布局。 |

触控有效目标至少 44×44px，不能因桌面宽度假定没有触屏。主内容自然纵向滚动，侧栏可独立滚动；日志与复杂表格只在自身容器横向滚动。320px 下不出现整页横向溢出，动作按钮与长错误允许换行。

顶栏吸顶用不透明底与分隔线，同页最多再加一条必要的操作区。手机固定导航或保存区需预留对应安全区，不能遮住焦点、错误或提交按钮。

## 6. 共享组件

| 组件 | 管理员端规则 |
| --- | --- |
| 按钮 | 常规 40px、紧凑 36px、触控 44px；主色实心、次级白底描边、轻操作透明。动作写具体对象，如“保存来源”“发布配置”“重试投递”。 |
| 面板 | 白底、细边框、12px 圆角，优先平面分区；不将每个字段包成独立卡片。 |
| 导航 | 选中项主色浅底、600 字重及 `aria-current`，可配指示条；图标统一线性 16–20px。 |
| 表格 | 表头次级底、最小高 44px，数据行最小高 56px；单元格横向 16px / 纵向 12px，仅横向分隔。数字右对齐，状态与操作可扫描。 |
| 表单 | 标签置顶，输入框 40px，手机文字 16px；大多数设置单列，相关短字段可并排。错误靠近字段并关联 `aria-describedby`。 |
| 筛选 | 显示搜索范围，保留其他筛选及返回状态；清除输入不删除记录。不把当前页筛选冒充全部数据搜索。 |
| 徽章 | 同时有短状态名与语义色，最小高 24px；草稿、未启用、已取消用中性色。 |
| Tabs | 40px 高，当前项有主色文字和 2px 下划线；按实际角色使用导航链接、tab 或分段按钮。 |
| 弹窗 | 短确认 400px、表单 560px、复杂预览 720px；最大宽为视口减 32px，有标题和关闭按钮，长内容可滚动。 |
| 菜单 | 200–280px 宽、最小触控行高 44px；选值用 Select，执行动作用菜单，支持键盘进入与退出。 |
| 空态 / 错误 | 区分无数据、筛选无结果、权限不足、服务失败；提供对应恢复动作。加载失败不能写为“暂无记录”。 |
| Toast / Alert | 成功可短时提示，必须处理的错误和异步结果留在页面/记录；失败保留数据，不靠转瞬提示。 |

所有组件具备默认、悬停、焦点、按下、禁用、提交中和错误状态。悬停不改变尺寸或位移。焦点使用 2px 主色轮廓与 3px 外偏移；图标按钮有可访问名称。链接负责导航，按钮负责状态修改。

## 7. 管理页面模板

| 页面 | 内容与操作规则 |
| --- | --- |
| 概览 `/overview` | 先展示有意义的运行、采集、投递异常与进入明细的路径。计数显示范围和时间，无样本时不用虚假成功率填空。 |
| 新闻来源 `/sources` | 类型、来源标识/URL、启用、更新间隔、最近成功、下次采集与状态；NewsNow 不同来源保留不同周期，RSS 单独配置。编辑失败保留输入。 |
| 来源详情 `/sources/:id` | 验证、手动采集、缓存/本地快照信息及错误；状态与真实动作分开，未验证来源明确标记。 |
| 模型配置 `/models` | 提供商、模型 ID、endpoint、key 引用、角色、启用与连接验证。列表明确显示深度思考开关状态；编辑弹窗用复选框设置，新增模型与未配置的旧模型默认关闭。说明保存后影响后续运行，开启要求模型支持，耗时及 Token 用量可能增加。密钥不回显，测试由明确按钮触发。主/摘要/评估/子模型角色可辨认。 |
| Agent 配置 `/agent-configs` | 版本、草稿与已发布状态、模型与预算、工具、系统提示词、子 Agent；保留差异预览与发布入口，已发布版本只读，新草稿另行保存。 |
| 运行 `/runs`、`/runs/:id` | 状态/模型/时间筛选，父子任务、事件时间线、实际用量与错误；状态不由前端事件数量猜测。 |
| 评估 `/evaluations` | 固定样例、运行实验、配置版本及 A/B 比较；评估模型未配置/不可用时给出修复路径，未评分显示空值。 |
| 用户 `/users` | 匿名账户的创建/最近活跃、访问频率、每日生成、并发、限流与风险状态；脱敏来源、封禁/解除及策略调整，显示审计事件和数据范围。 |
| 投递 `/deliveries` | 简报版本、站内目标、尝试次数与故障；当前仅支持站内发布，重试沿用同一期内容，不触发新生成。 |
| 登录 `/login` | 管理身份的独立入口与明确权限拒绝，原型提示不冒充服务端鉴权。 |

## 8. 运行监测与评估语义

时间线记录事件 ID、时间、动作、状态、耗时、脱敏参数和结果摘要；支持工具重复调用与父子运行关系。消息修复、摘要压缩、文件版本冲突、预算耗尽可作为事件查看，不预设每次运行固定顺序。

不展示模型私有思考过程，不泄露 API key、其他用户数据或未授权提示词。日志默认浅底等宽字体，长 JSON 折叠并局部滚动。新事件稳定追加；查看旧事件时不强制滚到底部。

流连接断开显示“连接中断，任务状态待确认”，不能直接判失败；重连补齐事件。取消先为“正在取消”，收到执行层确认后为“已取消”。无法确定总量时只显示状态、耗时与事件，不以预算消耗冒充完成百分比。

评估区别规则检查、模型评分和人工标注；比较对象显示配置版本、样例范围及指标定义。无结果显示待评分，不造 0 分；评分失败独立呈现，不能冒充任务完成。

生成成功与站内发布成功分开；站内通知写入成功显示“已发布”，不表示用户已阅读。写入失败或状态未知如实展示；重试沿用原简报，重新生成是另一项操作。当前只支持站内发布。原型全部事件、计数、评分和发布结果继续标明模拟。

## 9. 表单、版本与安全操作

输入前不满屏报错；提交校验定位到首个错误，服务端字段错误映射回字段，失败保留非秘密输入。`aria-invalid` 与错误说明关联。修改中的参数为草稿，开关按其实际语义说明立即生效或保存后生效。

未保存离开给出保留/放弃选择，保存成功反馈新版本或确认状态。配置发布说明影响后续运行，现有运行的配置快照不能随表单变化。敏感凭据只录入和提交，不在列表、日志、URL 或截图中回显。

删除或不可逆动作说明对象和影响，默认焦点放在安全动作。仅保存成功不弹确认框，不将日常可逆操作变成连续弹窗。

## 10. 可访问性与动效

语义化导航、主内容、标题、表单、表格；提供跳转主内容。所有关键操作可键盘完成，弹窗焦点进入、限制及退出后返回触发元素，Escape 遵循未保存提示。不能只靠颜色、悬停或图标识别结果。

常规微交互 120ms、菜单 180ms、弹窗 220ms；遵循 `prefers-reduced-motion`。无大幅视差、列表依次飞入或“AI 正在思考”的伪进度动画。图片或图标加载失败不影响操作含义。

320px 重排及 200% 文字缩放下，长模型 ID、URL、错误、按钮、状态、表格和时间线仍可读。模态与菜单不能溢出视口，软键盘不遮住保存入口。

## 11. 实现与验收

管理员端沿用共享基础组件和深青绿默认主题；用户端通过自己的主题与壳实现 `design-user.md`。共享类型和行为不需要为两种视觉复制。修改共享组件时分别检查双端，不能用用户视觉覆盖管理员全局变量。

完成页面时记录实际覆盖的状态、响应式尺寸、键盘操作及真实后端尚未验证的部分。检查来源编辑、模型连接、配置发布、运行取消、评估比较和投递重试；不得把 CSS 或模拟点击通过描述为真实 Agent 能力验收。

## 12. 管理员端 CSS 设计变量

下列变量保留原型 r1 值，与 `docs/releases/v1.0.0/draft/prototype/shared/tokens.css` 的基础主题一致。部分宽度与字号变量供共享原语复用，不表示后台必须增加阅读页面。基础控件样式仍由共享组件统一实现；本节不替代焦点、校验和异步逻辑。

```css
:root {
  color-scheme: light;

  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI",
    "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
    "Noto Sans CJK SC", sans-serif;
  --font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas,
    "Liberation Mono", monospace;

  --color-primary: #0F766E;
  --color-primary-hover: #115E59;
  --color-primary-active: #134E4A;
  --color-primary-soft: #E8F5F1;
  --color-primary-border: #B7DCD2;
  --color-accent: #93611F;
  --color-accent-soft: #FFF8E6;

  --color-bg: #F7F8F5;
  --color-surface: #FFFFFF;
  --color-surface-muted: #F1F3EF;
  --color-text: #202721;
  --color-text-secondary: #515C55;
  --color-text-muted: #66716A;
  --color-text-inverse: #FFFFFF;
  --color-text-disabled: #89938C;
  --color-disabled-bg: #EDF0EC;
  --color-border: #DFE5DE;
  --color-border-control: #7E8982;
  --color-overlay: rgba(20, 28, 23, 0.38);

  --color-success: #166534;
  --color-success-bg: #EDF7ED;
  --color-success-border: #C5E4CA;
  --color-warning: #854D0E;
  --color-warning-bg: #FFF8E6;
  --color-warning-border: #EDD79A;
  --color-danger: #B42318;
  --color-danger-bg: #FFF0ED;
  --color-danger-border: #F4C7BE;
  --color-info: #1D4ED8;
  --color-info-bg: #EEF4FF;
  --color-info-border: #CAD9FA;

  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-5: 1.25rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-10: 2.5rem;
  --space-12: 3rem;
  --space-16: 4rem;

  --radius-sm: 0.375rem;
  --radius-control: 0.5rem;
  --radius-card: 0.75rem;
  --radius-dialog: 1rem;
  --radius-pill: 999px;
  --border-width: 1px;

  --shadow-none: none;
  --shadow-popover: 0 8px 24px rgba(20, 28, 23, 0.10),
    0 2px 6px rgba(20, 28, 23, 0.04);
  --shadow-dialog: 0 24px 64px rgba(20, 28, 23, 0.18);

  --font-size-xs: 0.75rem;
  --font-size-meta: 0.8125rem;
  --font-size-ui: 0.875rem;
  --font-size-body: 1rem;
  --font-size-news-title: 1.125rem;
  --font-size-section: 1.25rem;
  --font-size-page-title: 1.75rem;
  --font-size-article-title: 2rem;
  --font-size-reading: 1.125rem;
  --line-height-body: 1.625;
  --line-height-reading: 1.7777778;

  --control-height-sm: 2.25rem;
  --control-height: 2.5rem;
  --control-height-lg: 2.75rem;
  --touch-target: 2.75rem;
  --topbar-height: 4rem;
  --sidebar-width: 15rem;
  --sidebar-rail-width: 4.5rem;
  --support-width: 17.5rem;
  --width-page: 80rem;
  --width-feed: 56.25rem;
  --width-reading: 47.5rem;
  --width-form: 47.5rem;
  --page-padding: var(--space-6);

  --z-sticky: 10;
  --z-popover: 20;
  --z-modal: 40;
  --z-modal-popover: 50;
  --z-toast: 60;
  --duration-fast: 120ms;
  --duration-normal: 180ms;
  --duration-slow: 220ms;
  --ease-standard: cubic-bezier(0.2, 0, 0, 1);
}
```

## 13. 变更与规范路由

项目指令按前端归属读取规范：用户端读取 `design-user.md`，管理员端读取 `design.md`，同时影响双端的组件读取两份。调整整体视觉时先更新对应文档，再同步其主题和组件。

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 1.0 | 2026-09-18 | 建立原型 r1 统一浅色体系、基础组件和交互规则。 |
| 2.0 | 2026-09-18 | 本文件改为管理员端专用，保留深青绿变量与侧栏框架，补全配置、监测、评估模板；用户风格独立到 `design-user.md`。 |
| 2.1 | 2026-09-18 | r3 统一为站内发布语义：写入成功为已发布，不表示已读；布局与色板保持不变。 |

## r5 匿名账户监测

匿名账户默认具有完整用户端权限；管理员登录仍是独立入口。用户管理展示匿名账户ID、状态、最近活跃、每分钟请求、今日生成次数、并发任务、限流命中与风险标记。风险状态有文字与依据，不只显示颜色。

详情按需展示脱敏IP、时间、配额和拦截原因；不展示会话Cookie或凭据。封禁需填写原因，明确影响该匿名账户后续访问；解除后显示审计结果。限流策略表单包含每分钟请求、每日生成、并发上限、同IP每小时新账户数，配置有校验和保存结果。模拟异常操作与真实记录明确区分，原型不声称部署级防护。

| 2.2 | 2026-09-18 | 新增匿名账户与异常访问监测、限流策略及封禁审计。 |
