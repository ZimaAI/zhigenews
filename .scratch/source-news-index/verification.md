# 来源新闻索引与自主检索验证

日期：2026-09-19。审查基点：da14e88afaef6674e0707cf9881bf1f86a0f90fe。

## 行为测试

- 采集与调度：40 项通过。覆盖发布时间窗口与边界、未知/未来排除、上游轮转累计与去重、失败/304/内容未变化维护、实际采集完成时钟、配置隔离、索引原子发布及租约。
- 运行与固定评估：18 项通过。使用模拟模型驱动真实 create_agent、MySQL、工具和发布流程；覆盖新闻目录检索、实际开始时钟、禁用来源不可发现、没有全量首消息、固定评估与原有取消/发布幂等行为。
- Harness：首次模型请求在 1 条与 1000 条私有证据下内容相同；自主读取索引和正文后合法引用通过；未知、无效、无时区、过期与未来发布时间均排除；子 Agent 沿用同一时间窗口和授权来源。
- 文件与沙箱：已验证真实 Docker Linux 新闻只读、整个本次工作区可写、目录隔离、网络限制、路径越界与文件工具 CAS；Linux root-owned 工作区经权限准备后，非 root 进程可以实际创建/覆盖文件。

## 完整后端检查

在 backend 虚拟环境中启用 HARNESS_TEST_MYSQL_URL 和 HARNESS_TEST_DOCKER，最终执行全部 tests：**218 passed，1 skipped**，耗时 74.99 秒。该次完整运行已包含真实采集文件的 Linux 非 root 读取补测。

唯一跳过项：隔离数据库迁移测试需要单独配置 MIGRATION_ADMIN_DATABASE_URL；本次没有数据库迁移。其余 MySQL checkpoint/恢复与 Docker 用例均已运行。

Ruff 对 backend/src 与 backend/tests 检查通过；Python compileall 通过。仓库后端未配置独立类型检查器。

## 集成发现与补测

首轮完整测试后补充真实采集文件的 Linux 读取验证：采集端临时文件默认 0600，导致非 root 沙箱读取 index.json 被拒绝。新增用例复现失败后，新闻文件在原子替换前设为 0644；真实 Linux root 采集生成的索引、parsed 与 raw 文件，均已由 UID/GID 65534 成功读取。该补测与采集 24 项测试复验通过，并已纳入最终完整测试；新闻挂载仍保持只读。

## 双轴审查

Standards：0 项硬违规、1 项低优先级路径解析去重建议；Spec：0 项发现。完整报告见 [review.md](review.md)。

## 验证范围

模型与上游 HTTP 使用明确的模拟输入；验证执行流程、来源引用和权限，不声明真实供应商的新闻选择质量或实际 token 费用。没有执行真实推送或部署。
