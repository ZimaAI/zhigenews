# 简报生成与阅读体验验收记录

日期：2026-09-21。范围：正式应用用户端、简报生成后端及其公开接口。

## 自动验证

- 后端累计 139 个不同测试通过：execution、harness_runtime、thinking_models、public_brief、api、runtime_configuration、execution_evaluation。
- 初次回归 129 passed / 2 failed / 1 skipped；两项失败均为旧模拟响应缺少新增必填 summary。更新合法响应样例，增加缺少导语拒绝输出测试后，thinking_models 21 项通过。
- 增加 6 项真实阶段与公开错误投影测试，public_brief 7 项通过。
- 原先缺少 HARNESS_TEST_MYSQL_URL 而跳过的 MySQL 取消恢复测试，配置隔离数据库后单独补跑通过。上述累计数量按不同测试去重，不把重跑计为新测试。
- 后端集成测试使用临时数据库和存储；未使用真实模型或向外部追踪服务发送测试数据。测试结束删除该次临时数据库。
- 用户端任务跟踪测试 5 项通过：恢复当前任务、断连自动重连、成功内容加载失败恢复、取消竞态、限流与身份错误、卸载清理。
- API 客户端测试 9 项通过。
- `npm run build` 通过：契约一致性、Vue 类型检查、用户端与管理员端构建。
- 最后一次仅调整进度契约字段说明后重新生成客户端，`generate-contract.mjs --check` 通过。接口结构未再变化。
- 修改的 Python 文件 Ruff 检查与 `git diff --check` 通过。

## 浏览器验证

通过 `.scratch/reader-brief-experience/browser.mjs` 加载真实用户端代码，页顶明确标注“模拟验收环境 · 新闻与任务均为模拟”。该模拟服务器验收后已停止。

- 运行中显示“正在查找和整理新闻”，等待较久显示“仍在整理，请稍候”；持续等待动效运行，没有百分比、预计时间或原始错误。
- 切换到连接失败后显示“正在重新连接”，旧新闻仍可浏览；恢复后自动显示“正在保存简报”。
- 刷新后恢复同一任务及真实阶段。
- 空结果显示“本次未发现符合偏好的新内容”，旧简报保留。
- 内部 partial 在今日页显示“简报已更新”，不展示附加说明；历史列表仅保留日期、标题和条数。
- 失败仅显示“生成失败，请重试”；模拟的内部错误串未出现在页面。重新生成按钮可启动模拟新任务，进行中防止重复提交。
- 详情显示独立读者导语、通俗新闻摘要与来源；Enter 打开新闻弹窗，Escape 关闭，焦点回到阅读按钮。
- 320、375、768、1024、1440、1920 CSS px 下验证页面无横向溢出；查看手机与桌面截图，布局正常。
- 控制台未发现页面脚本错误；状态使用 polite live region，动效含 prefers-reduced-motion 规则。系统级减少动效偏好未单独切换验收。

## 本地服务

- 确认没有活动生成任务后，重建 `zhigenews-backend:1.0.0` 并更新本地 API、Worker、Beat；不改数据库历史内容。
- `/healthz` 返回 `status: ok`，Worker ping 返回 pong。
- 容器内确认公开进度包含 phase、结构化导语为必填字段、读者写作提示词已加载。
- 正在运行的 5173 用户端开发服务已提供新进度组件。

## 验证边界

新闻内容和模型输出均为模拟样例；验证了结构化输出、保存映射、流程及界面。未运行新的真实模型生成来评判报纸式标题和白话摘要的实际写作质量。新写作规则只用于新生成内容，旧标题、导语与新闻摘要保持原样。
