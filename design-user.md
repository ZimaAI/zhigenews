# 知更用户端界面设计规范

> 版本：3.3 · 日期：2026-09-21 · 视觉基础原型：ITERATE r5；生成反馈与阅读规则适用于正式应用
>
> 风格：清爽的订阅发现与专注阅读
>
> 适用范围：仅用户端 `user-web/` 的页面、布局和主题。管理员端遵循根目录 [design.md](design.md)。

## 1. 设计依据与边界

本轮依据用户提供的参考截图：浅灰外背景包围白色内容画布，顶部图标导航以绿色下划线表示当前项，搜索框下方为明亮主题封面，随后是轻边框的主题选择卡片。截图用于确定视觉与信息层级，不是需要逐像素复制的产品，不复制其中品牌、标签名称或照片。

本系统保留今日简报、简报历史和个人设置。兴趣订阅与推送时间从头像进入个人设置；首次进入先完成订阅偏好。截图中的 Twitter、Reddit、Newsletters、Google News 入口不代表本系统已接入这些来源。当前订阅新闻来源仅为 RSS，Tavily 用于独立的联网补充搜索。历史原型中的来源均为模拟。

r2 的主题封面使用原创或明确可用的本地素材，只表达订阅方向，不是新闻现场照片、报道证据或来源 logo。主题标题和选择状态由真实 HTML 文字呈现；素材不可用时仍能识别和选择主题。

新增或修改用户端页面前完整阅读本文件，再检查已有用户壳、共享组件和全局样式。优先复用已有行为与语义组件；用户主题只在用户项目加载。管理员端不可因共享样式变动被同步改成阅读器布局。

## 2. 视觉原则

- 以白色画布、留白和细分隔线组织内容，页面外围使用浅冷灰。
- 顶部横向图标导航取代桌面固定侧栏。选中项使用文字、2px 绿色下划线及 `aria-current`，不只改变图标颜色。
- 页面文字只保留标题、内容、字段标签、必要状态与操作；不重复展示操作教学、版本说明、口号或模拟说明。
- 个人设置只包含订阅偏好和推送时间。偏好仅话题、背景、关键词，采用紧凑的可选择话题；大封面、分类和搜索不再占据设置页面。
- 今日简报、历史和详情保持文字优先，不把主题探索封面复制为每条新闻配图。
- 小圆角、轻边界、紧凑标题配合宽松内容间距。不使用厚阴影卡片堆叠、大面积品牌色、霓虹渐变或悬停弹跳。
- r4 恢复 r2 的线性图标、明确按钮层级和轻量标签，让简约页面有可识别的视觉细节；保留 r3 的页面结构、三字段偏好和单项时间设置。
- 保留新闻来源、发布时间、必要错误与恢复操作。用户端不暴露 Agent 参数、投递渠道或技术配置；用清楚标签与直接反馈代替说明段落。

## 3. 用户端设计变量

颜色只在用户主题入口集中定义，页面和组件引用语义变量。用户端可复用共享样式的变量名称，但只覆盖自身应用，不修改管理员端默认值。

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| `--color-bg` | `#F3F4F4` | 画布外围的浅冷灰背景。 |
| `--color-surface` | `#FFFFFF` | 主画布、表单、弹窗。 |
| `--color-surface-muted` | `#F7F8F8` | 次要容器、悬停与骨架。 |
| `--color-brand` | `#20B24B` | 装饰强调、导航下划线、主题选中指示。 |
| `--color-primary` | `#16803C` | 可读的深绿链接、主按钮、键盘焦点。 |
| `--color-primary-hover` | `#126C33` | 主操作悬停。 |
| `--color-primary-active` | `#0E5729` | 主操作按下。 |
| `--color-primary-soft` | `#EDF8EF` | 已选主题、订阅摘要的轻底色。 |
| `--color-primary-border` | `#BDDCC5` | 绿色浅容器的装饰性边界。 |
| `--color-text` | `#292D32` | 标题、正文。 |
| `--color-text-secondary` | `#62686D` | 次正文、说明。 |
| `--color-text-muted` | `#697075` | 来源、时间、帮助文字；在白色与浅灰背景上保持可读对比。 |
| `--color-border` | `#E5E7E8` | 分隔线与非交互容器边框。 |
| `--color-border-control` | `#89928B` | 表单控件和未选中控件的边界。 |
| `--color-text-inverse` | `#FFFFFF` | 深色按钮文字。 |

`#20B24B` 不能直接用于白底小字号正文，也不作为白字主按钮底色；亮绿承担装饰，深绿承担可读文字和主要操作。重要交互边界使用深绿或控件边框，不能只依赖浅分隔线。

成功、警告、错误和信息状态沿用共享语义色：成功 `#166534` / `#EDF7ED`，警告 `#854D0E` / `#FFF8E6`，错误 `#B42318` / `#FFF0ED`，信息 `#1D4ED8` / `#EEF4FF`。同时提供状态文字；普通主题不使用错误或警告色分类。

正文和说明文字以 4.5:1 为对比度检查目标；控件关键边界、焦点及状态指示以 3:1 为目标。具体组合应在实现中验证，不把上述色表当成整站已通过无障碍验收。

## 4. 字体、尺度与材质

字体使用现有系统无衬线：`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif`。仅运行编号和技术参数使用系统等宽字体，不依赖网络字体。

| 对象 | 字号 / 行高 | 字重 |
| --- | --- | --- |
| 页面标题 | 28 / 36px；手机 24 / 32px | 700 |
| 简报详情标题 | 32 / 44px；手机 26 / 36px | 700 |
| 分区标题 | 20 / 28px | 600 |
| 新闻标题 | 18 / 28px | 600 |
| 正文 / 摘要 | 16 / 26px | 400 |
| 长篇阅读正文 | 18 / 32px；手机 17 / 30px | 400 |
| 导航 / 按钮 / 字段标签 | 14 / 20px | 500 |
| 来源 / 帮助 | 13 / 20px | 400 |

以 4px 网格组织间距：4、8、12、16、20、24、32、40、48、64px。字段组间 24px，大分区间 32–40px，标题到内容 24px。标签与输入框间 8px，错误说明与控件间 4px。

按钮和输入框圆角 4px，普通卡片和主题封面 6px，弹窗 8px。桌面主画布可用轻微外阴影，内容卡默认无阴影；不要叠加多层厚阴影。常规控件高 40px，主要搜索框高 48px，触控控件有效命中区至少 44×44px。手机输入文字不小于 16px。

### 图标、按钮与标签

沿用 Lucide 线性图标：导航 18px、按钮 16px、标签 12–14px，描边约 1.8px。主导航和个人设置页签配语义图标；保存、添加、阅读等动作保留文字配图标。实心绿色是主操作，次操作用白底细边框，轻操作用透明底。图标只用于辨认动作或对象，不给每句文字增加装饰。

话题选项配对应的 Bot、BrainCircuit、Blocks、Lightbulb、ScanEye、Cpu、ShieldCheck、FlaskConical 图标；用浅底图标容器和选中勾选区分层级，继续保持紧凑网格。关键词采用带 Hash 图标的轻边框圆角标签；新闻话题采用淡绿色胶囊标签，来源仅显示名称，不加首字母方块。“更新简报”使用原方案的 WandSparkles 魔杖星光图标。数量与已保存状态可用低强调标签，不增加新统计或重复说明。颜色使用既有绿色、灰色语义变量，不为每个话题随机配色。

纯装饰图标设 aria-hidden；图标不能代替控件名称、已选状态和错误文字。移动端保留导航图标并控制间距，不通过隐藏文字容纳装饰。按钮/标签允许合理换行，点击区域与键盘焦点继续遵循原规则。

### 品牌标志

知更使用原创绿色知更鸟 SVG：向右上方飞翔、舒展双翼、轻盈的分叉尾羽与浅绿胸腹，保持简约、清新的轮廓。品牌固定使用薄荷绿 `#83CFAA`、翅膀绿 `#64BC94`、浅绿 `#B5E4CA` 与胸腹色 `#ECF8EF`，不随双端操作主题变色。源文件为 `packages/ui/src/assets/zhige-robin.svg`，页面通过共享 `BrandLogo` 组件引用，浏览器标签直接引用同一素材。

用户登录／匿名欢迎页、阅读器页头（首页、历史、详情、首次引导、设置）统一使用此标志配“知更”文字。页头图标 40px，欢迎页卡片 48px；不拉伸、不添加阴影或动效。邻近品牌文字时图像为装饰，独立使用时提供“知更”可访问名称；品牌导航保持至少 44px 点击高度。

## 5. 页面框架与响应式

桌面使用居中的白色主画布，最大宽度 1120px，外围留出浅灰背景。画布内部横向内边距 48px，顶部由轻量产品栏与横向主导航组成，内容自然纵向滚动。长篇正文仍限制为 760px，新闻流可为 900px，避免宽屏每行过长。

导航使用一套线性图标与真实路由链接。主导航只有“今日简报、简报历史”。点击头像直接进入 `/settings` 个人设置，不弹出中间菜单；设置内分为“订阅偏好”和“推送时间”。原型标记和演示场景入口保持轻量。

| 宽度 | 布局 |
| --- | --- |
| `≥1280px` | 1120px 居中画布，外围显露灰底；内容页保留阅读宽度，设置表单收窄便于填写。 |
| `768–1279px` | 画布随视口收缩；1024px 以下内部留 32px，以上 48px；横向导航保持文字，必要时局部横向滚动。 |
| `480–767px` | 内容内边距 16px；话题选项自然换行，表单按内容顺序单列。 |
| `<480px` | 内容内边距 16px；话题选项和按钮允许换行，主导航保持可见。 |

整页不能横向溢出。允许导航、复杂表格或日志在自身容器内滚动，不能用 `overflow-x: hidden` 隐藏不可达的操作。320px 下标题、关键词、表单错误与保存操作完整可用。无桌面固定侧栏；手机也不重复堆叠另一套固定导航遮挡内容。

## 6. 个人设置与首次使用

```text
主导航：今日简报 / 简报历史                         用户头像
个人设置
订阅偏好 / 推送时间

订阅偏好：话题多选 → 背景 → 关键词 → 保存
推送时间：每日时间 → 保存
```

话题使用轻量选项，选中同时体现文字、勾选或边框，按钮通过 `aria-pressed` 表示状态并支持 Enter/Space。背景使用有标签的文本域；关键词支持添加和删除。只修改草稿，明确保存后才生效；提交中防重复，成功短提示，失败保留草稿，未保存离开需确认。

偏好表单仅包含话题、背景、关键词；有效订阅至少一个话题或关键词。背景可为空、最多 500 字；最多 20 个关键词，每个最多 40 字。删除排除词、匹配模式、来源、语言、时间窗口、条数、摘要深度和记忆管理等用户控件。这些删减是用户在 r3 的明确要求，替代 r2 的完整参数表单。

推送表单只保留每天自动推送时间，使用系统时区（当前 Asia/Shanghai），默认 08:00。只支持站内简报，不提供邮件推送、目的地、验证、测试发送、渠道选择、暂停开关或用户时区选择。匿名账号无需邮箱或密码。

未完成首次偏好的用户进入 `/onboarding`：同一页设置话题、背景、关键词，主操作“开始阅读”。保存成功后持久化完成标记并进入今日简报；失败不标记完成。刷新和后续登录不重复引导。Cookie 对应的匿名账户自动恢复原偏好；Cookie 丢失或过期创建新身份并重新引导。首次引导不包含推送步骤，也不堆叠功能介绍。

## 7. 其他用户页面

| 页面 | 视觉与行为 |
| --- | --- |
| 今日简报 | 日期、话题筛选和新闻列表；无已读/未读功能。生成展示当前真实阶段、持续轻量动效和简短结果，去掉完成比例与预计剩余时间，新生成时旧简报可读。 |
| 简报详情 | 保留整期阅读；点击单条新闻标题或阅读操作打开该条详情弹窗，展示摘要与原文来源链接，不展示推荐理由或关联来源列表。无 Agent 快照或整理记录。 |
| 历史 | 紧凑列表与必要搜索/筛选；日期、标题、条数清楚，不显示生成状态标签（包括部分完成）、邮件状态或重复描述。 |
| 生成反馈 | 只展示少量真实业务阶段与持续轻量动效，不展示百分比、预计剩余时间、模型、工具、执行步骤、事件或内部 run ID。同阶段耗时较长时提示“仍在整理，请稍候”。旧运行链接回到今日页。 |
| 匿名进入 | 恢复 r2 双栏欢迎画面与品牌视觉；进入网站自动建立匿名会话，成功跳转偏好或今日页。只显示加载、失败重试、必要访问受限提示，去掉邮箱、密码、注册表单。 |
| 个人设置 | 头像直达；订阅偏好和推送时间是两个简洁子页面，显示匿名身份，不提供会反复创建新账号的退出按钮。 |

新闻条目展示来源/时间、标题、摘要及阅读操作，不展示推荐理由或关联来源列表。参考链接必须可追溯，不记录已读/未读状态。全局保留一个清楚的模拟标记，单条不反复堆叠同样的说明；不能把参考资料伪装成真实当天报道。

新生成简报的标题用一句报纸式概括，导语用 1–2 句说明本期重点，单条新闻摘要用 2–3 句通俗语言说明主要事实与有依据的关注点。保留影响理解的必要限定与来源归属；不写检索窗口、索引数量、执行过程或后台限制说明。导语独立表达新闻内容，不以内部限制说明填充。既有历史标题与正文不自动改写。

可读简报生成成功后，今日页统一提示“简报已更新”，不区分内部完成与部分完成。正常结束但没有相关新闻时，提示“本次未发现符合偏好的新内容”，保留旧简报且不新增空白历史；无旧简报时呈现简洁空态。生成失败显示“生成失败，请重试”及重试入口，保留旧内容。进度连接中断显示“正在重新连接”，自动恢复后继续跟踪同一任务；刷新或返回页面同样恢复真实状态，不重复创建任务。

## 8. 组件状态与可访问性

所有数据区区分正常、加载、无内容、筛选无结果与请求失败；简报生成结果按上述规则处理，不展示内部部分完成等级。已显示内容在后台更新时保留；错误有明确恢复入口。提交时防重并提供文字反馈；失败保留输入，成功更新版本或状态。

保留页顶简短模拟标识、场景切换与重置入口。新闻、来源参考、运行事件和评分均保持 synthetic 语义。站内发布成功可标记“已发布”，不声称用户已阅读；失败与未知状态如实保留。

导航用链接，状态修改用按钮，所有输入有标签。提供跳转主内容入口，`:focus-visible` 使用 2px 深绿轮廓及 3px 外偏移。弹窗打开后焦点进入并受限，Escape 关闭时遵循未保存提示，关闭后回到触发元素。

常规动效仅用于颜色、轻微透明度与必要的浮层过渡，120–220ms；生成过程中允许持续轻量等待动效，遵循 `prefers-reduced-motion`，减少动效时仍保留可读状态文字。不使用卡片悬停上浮或滚动视差。生成阶段由公开业务状态提供，不能按时间、模型调用或工具次数虚构阶段推进；原型固定时间演示由全局模拟标记说明。

## 9. 实现与检查

用户布局与主题放入 `user-web/` 并由用户入口加载，基础类型、mock adapter 和无视觉冲突的组件仍可共享。需要修改共享组件行为时保持接口兼容，分别检查双端；不通过全局修改 `shared/tokens.css` 改变管理员风格。

每轮检查至少包含：首次引导与刷新、头像设置入口、话题与关键词选择、显式保存、失败后草稿保留、未保存离开、生成和阅读、推送时间保存、原型场景切换；检查 320、375、768、1024、1440、1920 CSS px 下关键页面，核对键盘焦点与移动换行。记录实际检查结果，不把本规范当成测试证据。

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 2.0 | 2026-09-18 | 用户端从原统一规范中独立；根据用户截图采用白色画布、横向导航、绿色强调与主题探索卡片。管理员端继续使用 `design.md`。 |
| 3.0 | 2026-09-18 | 精简用户端文字与导航；头像直达个人设置，偏好仅话题/背景/关键词，推送仅每日时间且仅站内，首次使用引导持久化。 |
| 3.1 | 2026-09-18 | 在 r3 简约结构中恢复线性导航与动作图标、话题图标、关键词/新闻标签；后续反馈移除来源字母方块，更新简报恢复魔杖星光图标；保持功能与字段不变。 |

### r5 匿名会话与新闻弹窗

匿名会话具备全部用户端功能，不能访问管理员功能。正常进入自动创建/恢复 Cookie 会话，不要求点击登录。欢迎页仅承担进入中的视觉和重试。Cookie 失效、封禁和频率限制给出相应结果；限流按服务端剩余等待时间提示，不通过不断新建账户重试。

单条详情弹窗桌面最大宽 760px，手机宽度不超过视口减 32px；标题/来源/时间/话题/正文清晰，长内容在弹窗内滚动，关闭按钮至少 44px。打开后焦点进入，Tab 留在弹窗、Esc 关闭、焦点返回触发按钮。背景不可操作；来源链接保留新标签提示。

| 3.2 | 2026-09-18 | 自动匿名进入与欢迎页，移除已读及整理记录，单条新闻详情弹窗，总体进度与预计剩余时间。 |
| 3.3 | 2026-09-21 | 正式应用采用真实阶段与持续轻量动效，去掉百分比和预计时间；简化生成结果、支持空结果与重连恢复、历史隐藏状态；新增面向读者的标题、导语与摘要规则。 |
