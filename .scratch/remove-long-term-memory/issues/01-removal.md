# 删除长期记忆实现及依赖

Type: task
Status: resolved
Blocked by: none

## Tasks

- 删除记忆写入、同步、Store 和模型上下文注入。
- 移除 API、客户端与原型模拟接口。
- 更新当前规格及回归测试，验证摘要、偏好、发布和 checkpoint 行为。

## Comments

用户明确授权本次删除。旧规格中的长期记忆要求由本次决定替代；不重写历史基线和交接快照。

## Answer

已删除偏好/简报记忆写入、执行前同步、UserMemory/Store 初始化与传递、记忆 API/客户端/原型模拟能力，以及摘要中间件中的记忆注入和预算。保留偏好版本、简报发布、会话摘要及 checkpoint；当前说明与规格已同步。

相关后端 131 个独立测试通过，包括本地 MySQL 的线程恢复和进程重启检查；API 客户端 9 项通过。正式应用与原型的契约、类型检查通过，Ruff 和差异格式检查通过。未运行与本次修改无关的 Docker 沙箱检查或真实外部模型/Tavily 调用。

已重建本地后端镜像并恢复 API、worker、beat。HTTP 健康检查为 200，记忆查询路由为 404，在线 OpenAPI 不含记忆路径。旧记忆数据库记录未删除，新实现不再读写。
