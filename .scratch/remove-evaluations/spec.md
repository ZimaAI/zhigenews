# 移除评估实验

按用户要求移除正式应用中的评估实验，未来重新实现。

范围：管理员导航和页面、固定样例管理、实验 API 和 DTO、规则/模型评分、执行与恢复任务、评估模型配置、对应测试及当前运行文档。新闻采集、简报生成和发布继续保留。

历史发布基线、交接证据和隔离的旧原型保留为历史资料，不是正式应用的功能。已有实验记录、样例和文件不执行数据清除；待发送评估任务不再进入调度批次，不阻塞有效任务。

验收：旧 API 返回 404；无评估页面或导航；生成契约一致；旧评估 outbox 不阻塞运行任务；现有后端回归、前端类型检查与构建通过。
