# 实现验证记录

日期：2026-09-20。审查基准：`1fa80f9d27a043c88ad2063d996a3d6e0c87cc0d`。

## 自动化验证

- 后端完整测试：`uv run python ../.scratch/admin-source-management/run_tests.py -q`，240 passed、7 skipped。首次运行暴露旧记录缺少 `nextFetch` 的兼容问题，修复后完整重跑通过。
- 审查修复后专项：同一隔离运行器执行 `tests/test_source_management.py tests/test_workers.py tests/test_ingestion.py -q`，47 passed；覆盖新增旧版失效排队记录恢复、采集并发、停止、物理清理、清理失败重试及退避。
- 前端 `npm run build` 通过，包含契约同步、类型检查、用户端与管理员端构建；最后恢复只读来源标识后 `npm run check` 再次通过。
- `npm run test:api`：9 passed。修改的后端文件 Ruff 检查通过；`git diff --check` 通过。
- 后端有一项 Starlette/AnyIO 弃用提示，不影响本轮测试。跳过的 7 项不计为通过。

测试运行器使用独立随机 MySQL 数据库、合成上游响应及临时文件，结束后删除该临时数据库。全量删除测试仅在运行器明确标记的可丢弃数据库中执行，避免普通测试环境误删既有来源。

## 浏览器验收

使用隔离 API 与临时管理员账户、25 条名称明确标注“模拟来源”的记录，不代表真实上游采集验收。

- 每页 10 条，三页分别 10、10、5 条；上一页、下一页、跳页及首末页禁用状态正确。
- 第一页与第二页分别选择来源，已选数跨页保留；批量停用显示成功 2 条，选择清零。
- 名称用 Enter 或搜索按钮提交；搜索跨页目标返回 1 条并回到第一页，条件变化清空选择。Space 可切换复选框。
- 删除全部失效来源确认仍显示数据库全量 2 条，包含已停用项，不受当前仅 1 条的名称筛选影响。确认完整说明历史快照、采集文件、评估重跑及生成中简报影响；Escape 可取消。实际物理删除由隔离 HTTP 集成测试验证。
- 排队采集可停止并停用。受控文件锁持有时显示“停止中”，释放后刷新为“已停止”。真实 worker 退出与删除互斥由线程集成测试验证。
- 空搜索结果正确禁用全部分页操作；停止隔离 API 后页面显示请求失败及重试入口，不冒充成功或空列表。
- 320px 视口表单和按钮换行，页面无横向溢出，表格局部滚动；测试后恢复默认视口。

## Standards

发现 1 项规范违反：来源列表移除了标识/URL，不符合 `design.md` 第 7 节。已恢复只读 `source.sourceId || source.url`，名称搜索范围保持不变。独立审查复核已关闭，无剩余发现；无需单独报告的代码异味。

## Spec

发现 1 项实现问题：旧版 `syncing` 且已有三次失败的来源会遗留排队状态，阻止手动恢复。不再允许自动执行的旧来源现在按实际文件锁投影运行／空闲状态。新增 HTTP 与 worker 回归先验证失败，再验证手动恢复成功；独立审查复核已关闭。

双轴审查：Standards 1 项已修复、剩余 0；Spec 1 项已修复、剩余 0。
